# Asset-Audit Remediation — Top-10 Track (Variation, Parcels, Sockets, Materials, Variants, Condition, Vegetation, Rooftops)

> **For agentic workers:** REQUIRED SUB-SKILL: Use **superpowers:subagent-driven-development** (recommended) or **superpowers:executing-plans** to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Burn down the audit's "do these first" top-10 (minus the two already done): cooked Forge buildings sit on real parcels (ground/driveway/fence/garden), consume their own sockets (front path, frontage, gate, chimney), vary per instance and per cooked variant (seeds, condition, merge-patch), draw every render path from one material truth, fill yards with cooked vegetation, and seat rooftop equipment on the actual roof.
**Done When:** `cd ~/Ochroma/projects/urban_horizon && cargo run --bin game_asset_cook && cargo run --release --bin city_coverage` — the cook prints `cooked city.res_med.l3.3x4.rowhouse_01.v1` and `.v2` lines (pool of 3 at level 3), per-asset `plot:` lines with non-zero ground/fence counts, and a `flora:` line for ≥3 cooked species; `city_coverage` writes `play_houses_closeup.png` / `play_houses_front_closeup.png` in which a human sees two adjacent craftsman lots that are **no longer bit-identical** (one mirrored, tint-shifted, setback-jittered) on dressed parcels — fence with a gate gap, driveway, paver path running from the road to the actual door, 1–2 yard trees — and `play_school_closeup.png` shows RTU/vent boxes seated ON the school roof instead of empty slab. Read-only confirmation: the **forge_pathtrace** binary (env recipe in IMPORTANT NOTES) re-renders the recooked payloads and prints its `gates:` line.
**Architecture:** Everything rides the proven cook→payload→placement path. Cook-side (game_asset_cook.rs): derive top-level materials FROM material_zones, re-anchor rooftop atoms to mesh-derived roof_top/eave_top, plumb `condition` into the forge description, expand directives via RFC-7396 merge-patch variants, and bake forge-cli `plot` + `vegetation` output into payloads. Game-side (render_gpu placement): per-instance deterministic variation off `lot.seed`, and socket consumers (path, frontage, chimney) at `place_ready_asset_splats` time. No new render pipeline; no sim changes.
**Design Document:** `~/Ochroma/projects/urban_horizon/docs/asset_audit_2026-06-10.md` (the verified 55-finding backlog; this plan executes its lead top-10 minus items 1 and 5 — see "Scope deltas" below).
**Tech Stack:** Rust (workspace toolchains as-is), serde_json, forge-cli subprocess (building/plot/vegetation), tiny_skia PNG out. No new crate deps — deterministic hashing is splitmix64-style inline (the repo has no `rand`; forge-plot's own Pcg64 is already seeded from `params.seed`).
**Build:** game `cd ~/Ochroma/projects/urban_horizon && cargo test`; cook `cargo run --bin game_asset_cook` (needs forge checkout + AssemblyPrime `uv` project discoverable, same as today's payloads); forge `cd ~/src/forge && cargo test -p forge-building` (plus `-p forge-plot -p forge-vegetation` if touched). Forge repo has **NO git** — edit with care.

---

## Scope deltas — top-10 items dropped as already done (verified in code 2026-06-10)

- **Top-10 item 1 (ALL-P0-1, re-point the QA harness): DONE.** `src/bin/forge_pathtrace.rs` is rewritten — header doc (lines 1–5) states every render comes from a cooked `ReadyAssetPayload` via `load_ready_asset_mesh` (line 443) + `ready_textured_materials` (line 709); `COOKED_SPECS` (lines 89–120) covers all 5 payloads (craftsman, rowhouse, victorian, school, police); pixel gates print `gates: PASS` / `gates: FAIL <view>` (lines 434–437) with calibrated `gate_band` bands (line 198) and a `FORGE_GATE_SELFTEST` corruption mode. The old fake RenderSpec/palette block is gone.
- **Top-10 item 5 (ALL-P1-1, normalize zone tints): DONE.** `src/asset/textures.rs` now mean-normalizes: `factor[c] = clamp(tint[c] / set_mean[c], 0.25, 4.0)` per channel (`load_set`, ~lines 290–295; bounds `TINT_FACTOR_MIN/MAX` ~lines 138–141; module doc lines 33–42), so the tinted texture's mean lands on the authored zone color — no double-darkening. The cook's atom sampler already consumes the normalized texels (`atomize_mesh_triangles` comment, game_asset_cook.rs ~lines 746–749).

Remaining top-10 items 2, 3, 4, 6, 7, 8, 9, 10 → Tasks 1–8 below (re-ordered cook-foundation-first).

---

## IMPORTANT NOTES

- **Repos & commits:** game `~/Ochroma/projects/urban_horizon` (git, uncommitted work present), forge `~/src/forge` (**NOT a git repo** — no commits possible, edit with care), engine `~/src/ochroma`. **Do NOT run `git commit` anywhere** — the user commits. Every task's final step is "leave uncommitted".
- **Ownership (hard boundaries):** this plan OWNS `src/render_gpu/**`, `src/bin/game_asset_cook.rs`, `src/building/compose.rs`, and may touch `~/src/forge` (contingency only — no forge edits are expected). It must **NOT** touch `src/game/**` sim logic (living-instances track) and must **NOT** edit `src/bin/forge_pathtrace.rs` (porch-closure agent owns it until released) — final visual verification runs that binary **READ-ONLY**: `LD_LIBRARY_PATH=$HOME/slang-sdk/lib SPECTRA_SLANG_DIR=$HOME/src/spectra/slang SPECTRA_BACKEND=vulkan VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json FORGE_PREVIEW_SPP=32 cargo run --release --features spectra --bin forge_pathtrace`. Its gate bands are calibrated against **today's** payloads — if a legitimate visual improvement drifts a band, report the per-view means to the porch-closure owner for recalibration; do not edit the harness. `src/asset/mod.rs` may receive **additive, serde-default** fields only (shared schema). Do not touch `src/lot/**` — the live placement path is render_gpu, not `compose_lot_building`.
- **Determinism:** every jitter/choice derives from `lot.seed` (`PlannedLot.seed: u64`, src/lot/layout.rs:82) or `recipe.seed` via an inline splitmix64-style hash. No `rand` crate in the game repo; no unseeded RNG anywhere. Same seed ⇒ bit-identical output (tested).
- **Placement signatures (render_gpu/mod.rs, verified today):**
  - `fn place_ready_asset_splats(catalog: &crate::asset::AssetCatalog, asset: &crate::asset::BuildingAsset, center: [f32; 2], yaw: f32, local_offset: Vec3) -> Option<Vec<GaussianSplat>>` (line 1289) — extend with a trailing `instance_seed: u64`; same for `place_ready_asset_mesh_renderables` (line 1338) and `place_ready_asset_detail_meshes` (line 1435). Callers: lot loop at line ~750 (`l: &PlannedLot` in scope — pass `l.seed`) and service loop at line ~854 (no lot — pass a hash of `b.position` bits).
  - `place_ready_asset_sdf_instance` (line 1319) gets position/yaw/setback variation but **NOT** mirror (`ResidentSdfVolume` has only `rotation_y`; building SDFs are near-symmetric — record as accepted drift, do not invent an SDF mirror).
  - `GaussianSplat::volume(position: [f32;3], scale: [f32;3], rotation: Quat, opacity: u8, spd: [u16;16])` and `.apply_transform(&mut self, position: Vec3, rotation: Quat, scale: Vec3)` (vox_core); `rgb_to_spectral(r, g, b) -> [u16;16]`.
- **Payload schema (src/asset/mod.rs, verified):** `ReadyAssetAtom { position: [f32;3], scale: [f32;3], color: [f32;3], opacity: u8, channel: Option<ReadyAssetMaterialChannel> }` (line 200). `ReadyAssetAssemblySocket { id, kind, channel, position, normal, compatible_tags }` (line 357) — payloads carry `door_front`, `path_start`, `gate_post_left/right`, `chimney_top`, 4× `cornice_seat`. `ReadyAssetSnapPoint` kinds include `RoadFrontage` (line 267). Zonable pools: `pool[(seed % pool.len() as u64) as usize]` in `zonable_for` (line 567) and `zonable_for_theme` (line 602) — cooked variants grow these automatically once registered in `pack.json`.
- **door_front socket sits at facade center but the real door is up to 1.65 m off-center (audit ALL-P1-12).** Locate the true door game-side from door-material (id 6) mesh triangles — the read-only harness already does exactly this in `door_anchor` (forge_pathtrace.rs:477); replicate, do not import.
- **Cook signatures (src/bin/game_asset_cook.rs, verified):**
  - `fn atomize_building(recipe: &AssetRecipe, mesh: &ForgeMesh, forge_asset: Option<&ForgeAssetContract>, textures: &mut TextureCache) -> Vec<ReadyAssetAtom>` (line 621); the bad anchor is `body_top = bounds.height().max(floors*floor_height)` (lines 632–634). Detail fns: `add_house_details` (976), `add_rowhouse_details` (1096; floating duplicate slab ~1187–1206; 3 fake splat-only doors ~1170–1185), `add_school_details` (1227; fake double doors ~1250–1263), `add_police_details` (1274; floating beacon/antenna ~1299–1312) — all currently `(atoms, recipe, width, depth, body_top)`.
  - `fn materials_for(recipe: &AssetRecipe, source: &AssemblyPrimeExport) -> Vec<ReadyAssetPbrMaterial>` (line 1652) builds 8 palette-tinted slots; `AssemblyPrimeExport::textures_for` falls back to `self.polyhaven_textures.values().next()` — **nondeterministic HashMap order, must die** (lines 2744–2750).
  - `fn forge_description(recipe: &AssetRecipe) -> Value` (line 2492) hardcodes `"condition": "New"` (line 2499). Forge consumes it: `description::Condition { New, Aged, Weathered, Derelict }` (forge description.rs:72); `material.rs::wall_material` shifts desaturation 0/0.05/0.15/0.30 and roughness 0.6/0.7/0.8/0.95 and renames `material_id` to `{siding}_{condition}` — flows into zone tints → (normalized) atom colors with zero new plumbing. Displacement maps appear when condition ≠ New; game `resolve_uri` has no `_disp` arm — they are carried, unresolved, harmless (diffuse stem is unchanged).
  - `ForgeRunner` (line 2784), `generate_building` (2835) shells `forge-cli run '{"command":"building","params":{...}}'`. The CLI also exposes **`plot`** and **`vegetation`** (manifest.rs:103/138). Plot params: `archetype` (`residential_suburban|residential_urban|residential_rural|commercial|industrial|park|churchyard`), `footprint: [f32;2]`, `building_footprint: [f32;2]`, `condition`, `seed`, `fence_style` (`picket|chain_link|stone_wall|hedge|none`), `driveway_surface` (`gravel|concrete|asphalt|none`); returns `{ ground{positions,normals,uvs,indices,material_ids}, fence_instances[{position,rotation_y,asset_query}], driveway{...}, prop_placements(always empty), garden_zones[{center,radius,density,asset_query}] }`. Ground material ids: 0=foundation, 1=front path strip, 2=driveway, else lawn. Garden zones are Pcg64-seeded from `params.seed` — deterministic. **Verify the plot mesh's coordinate frame against the building's corner-origin frame before baking** (garden zone centers use a centered ±half frame); print both bounds in the task and align via the existing `forge_to_asset_local` convention.
  - Vegetation params: `{ seed, species, height, age, wind_lean }`, species serde snake_case: `pine_tree|spruce_tree|oak_tree|birch_tree|shrub|tall_grass|fern|cactus`; returns a ForgeVolume mesh.
  - `ForgeMaterialZone { id, triangle_start, triangle_count, material: ForgeMaterialSpec { material_id, base_color: [f32;4], roughness, metallic, base_color_map, roughness_map, normal_map, displacement_map } }` (line 2977); channel mapping `forge_zone_channel` (line 1386).
  - Variant merge-patch: forge's `vary.rs` (RFC 7396 → regenerate) is **NOT compiled** (`lib.rs` has no `pub mod vary`) — do not call it; reimplement RFC 7396 (~15 lines, `null` deletes a key) in the cook and patch the **directive JSON Value before deserializing** so floors/palette/condition/seed all patch through one mechanism.
- **Texture stems already provisioned** (textures.rs `STEM_SETS`, lines 49–66): `lawn→leafy_grass`, `asphalt→asphalt_02`, `concrete_pavers→concrete_pavers`, plus brick/plaster/siding variants from `polyhaven_fetch`. Plot/path baking uses `TextureCache::load(&mut self, uri: &str, tint: [f32;3]) -> Option<Arc<TextureData>>` exactly like `zone_diffuse_texture` (cook line 763).
- **HVAC kit recipe source:** `src/building/compose.rs::add_flat_roof` (lines 147–208) — parapet lip ring, seeded jitter, RTU block. Port the *recipe* (dimensions, seeding pattern) to cooked `ReadyAssetAtom`s; compose.rs itself stays unchanged (it serves the non-asset fallback path).
- **Forge material ids (per-triangle u8):** 0=wall, 1=roof, 2=glass, 3=reveal, 4=trim, 5=cornice, 6=door.
- **AssemblyPrime cost:** variants of one directive **reuse the base recipe's AssemblyPrime export** (one `uv` run per archetype, not per variant) — pass the base `source` into `materials_for` for each variant.
- `todo!()` / `unimplemented!()` / empty function bodies are **forbidden** — they fail the task. Every test asserts a real computed outcome; `assert!(x.is_some())` alone is forbidden.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `~/Ochroma/projects/urban_horizon/src/bin/game_asset_cook.rs` | roof_top/eave_top re-anchor + cooked HVAC kit (T1); zone-derived top-level materials, kill nondeterministic fallback, drop fake door blobs (T2); `condition` plumb (T3); RFC-7396 variant expansion + starter seed-variants (T4); `ForgeRunner::generate_plot` + parcel bake with gate break (T5); `ForgeRunner::generate_vegetation` + flora cooking (T8); tests in the in-file `mod tests` (line 3088) |
| Modify | `~/Ochroma/projects/urban_horizon/src/render_gpu/mod.rs` | socket consumers — door path, frontage orientation, chimney anchor (T6); `instance_variation` mirror/yaw/setback/tint off `lot.seed` (T7); garden-zone resolver + yard/garden vegetation instancing (T8); tests in the in-file `mod tests` (line 2830) |
| Modify | `~/Ochroma/projects/urban_horizon/src/asset/mod.rs` | **additive serde-default only:** `plot_garden_zones` passthrough on `ReadyAssetPayload`, `flora` section on `AssetPack` + catalog loading (T5, T8) |
| Modify | `~/Ochroma/projects/urban_horizon/assets/source/buildings/residential/rowhouse_01.asset.json` | `variants` merge-patch block — 2 patches (seed/floors/condition) ⇒ level-3 pool of 3 (T4) |
| Reference | `~/Ochroma/projects/urban_horizon/src/building/compose.rs` | `add_flat_roof` recipe source for T1 — read, do not modify |
| Reference (read-only binary) | `~/Ochroma/projects/urban_horizon/src/bin/forge_pathtrace.rs` | visual confirmation renders; NEVER edited by this track |
| Contingency | `~/src/forge/crates/{plot,vegetation}/**` | only if the CLI contract proves broken in practice; test with `cargo test -p forge-plot -p forge-vegetation`; NO git |

---

## Capabilities

| Capability | Real behavior test | Stub test (forbidden) |
|---|---|---|
| Rooftop seating | printed `max gap` between kit-atom bottoms and mesh roof top ≤ 0.15 m on a synthetic flat-roof mesh; floating-atom validation rejects a planted 0.5 m-high atom | `assert!(atoms.len() > 0)` |
| Material single truth | derived top-level facade material equals the facade zone's `material_id` + tint (3 decimals) from a synthetic contract; two cooks of the same input byte-identical | "materials list non-empty" |
| Condition | `forge_description` with `condition: "Weathered"` emits that string; zone passthrough keeps `clapboard_weathered` id and a measurably desaturated tint vs New | JSON parses |
| Variants | merge-patch unit test prints patched floors=4, condition="Aged", seed changed; expansion yields ids `…rowhouse_01`, `.v1`, `.v2` | patch returns Ok |
| Parcel bake | cook prints `plot: ground N tris, fence M posts (gate gap G)` with N>20, M>10, G≥2 for the craftsman; fence-break unit test prints kept/skipped counts | plot JSON parsed |
| Socket path | path splats lie within 0.5 m of the segment path_start→true-door (mat-6 triangles), count ≥ 6, printed endpoints | `assert!(sockets.len() > 0)` |
| Instance variation | seeds 1 vs 2: ≥30 % of atom x-positions differ (mirror) and mean |Δcolor| > 0.005; seed 1 twice: bit-identical | "splats returned Some" |
| Yard vegetation | developed res-low lot scene contains ≥ 40 Vegetation-channel splats outside the building bounds, printed count | flora file exists |

---

## Task 1: Re-anchor rooftop atoms to mesh roof_top/eave_top + port the HVAC kit (audit item 10: ALL-P1-4 + SCH-P1-1 + POL-P1-1 + ROW-P1-1, plus the ALL-P2-5 floating-atom gate)

**Files:** Modify `src/bin/game_asset_cook.rs` (anchors, all four `add_*_details`, `validate_quality`); tests in its `mod tests`.
**Acceptance:** `cd ~/Ochroma/projects/urban_horizon && cargo test --bin game_asset_cook rooftop -- --nocapture` → prints real numbers like `flat roof_top 7.02 eave_top 6.80, kit atoms 9, max seat gap 0.12` and a second test proves `validate_quality` REJECTS a payload with a rooftop atom floating 0.5 m (error message printed). Then a full `cargo run --bin game_asset_cook` recook succeeds with no floating-atom rejection.
**Wiring requirement:** `atomize_building` (line 621) computes `roof_top` (max y over material-id-1 triangles) and `eave_top` (max y over id-0 wall triangles) and passes both into every `add_*_details` call (lines 658–663); `validate_quality` (line 2153) enforces the new gates. `todo!()`/stubs = task failure.

- [x] Step 1: failing tests — (a) synthetic flat-roof ForgeMesh (slab top at a known y) through `atomize_building` asserting every Detail/Roof atom above the eave has `bottom = center.y - scale.y` within 0.15 m of roof_top AND ≥3 kit atoms exist; (b) `validate_quality` rejects a hand-built payload containing one atom whose bottom is 0.5 m above roof_top.
- [x] Step 2: run — both fail today (kit anchors off `body_top` which includes the roof; no floating gate exists).
- [x] Step 3: implement — `mesh_roof_top`/`mesh_eave_top` helpers; re-seat every rooftop atom in `add_house/rowhouse/school/police_details` on the computed anchors; delete the rowhouse floating duplicate roof slab (~1187–1206); port the `compose.rs::add_flat_roof` recipe as atoms for school/police/rowhouse — 2–3 RTU boxes (half-extents ≈ [1.25, 0.6, 0.75]) seated on roof_top, 3–6 vent stacks, a roof-access bulkhead near a corner, parapet-lip ring — count/positions jittered deterministically from `recipe.seed` (mirror the `jit` pattern in compose.rs:154–155). Victorian/craftsman chimney + dormer-cluster atoms grounded on the roof slope (audit VIC-P1-2 interim): chimney stack seated on the rear slope crossing the ridge by ~0.8 m; never emit window atoms for features with no geometry.
- [x] Step 4: wire in `atomize_building` and `validate_quality` exactly as stated; new gate: Flat-roof kinds require ≥3 rooftop Detail/Roof atoms with bottoms within 0.15 m of roof_top; reject any rooftop atom whose bottom is > 0.25 m above roof_top.
- [x] Step 5: tests green with printed non-trivial y values; full recook prints all `cooked …` lines (no gate failures). *(Printed: `flat roof_top 7.02 eave_top 6.80, kit atoms 99, max seat gap 0.12`; recook exit 0, all 5 `cooked …` lines.)*
- [x] Step 6: leave uncommitted.

## Task 2: Single material source of truth at cook (audit item 6: ALL-P0-2, + ALL-P1-7 fallback kill)

**Files:** Modify `src/bin/game_asset_cook.rs` (`materials_for`, `textures_for`, fake-door blobs in `add_*_details`); tests in `mod tests`.
**Acceptance:** `cargo test --bin game_asset_cook material_truth -- --nocapture` → prints `facade: castle_brick_02_red tint [0.55, 0.28, 0.20] (zone == top-level)` from a synthetic ForgeAssetContract, and a determinism test prints two identical serialized materials lists from two cook passes. Recook output includes per-asset `materials: K from zones, F fallback roles`.
**Wiring requirement:** `materials_for` (line 1652) takes the `ForgeAssetContract` zones (same source as `ready_material_zones`, line 1360) and derives each channel's top-level `ReadyAssetPbrMaterial` (id, tint, textures) from its zone; AssemblyPrime role textures remain ONLY for channels with no zone (Ground/Detail/Signage); the `values().next()` HashMap fallback (lines 2744–2750) is replaced by a fixed per-role default table + a printed warning. Fabricated splat-only door blobs (rowhouse ~1170–1185, school ~1250–1263, police equivalents) are deleted — door atoms exist only where mat-6 geometry exists; adjust `validate_quality` door/signage minimums to match reality.

- [x] Step 1: failing tests — zone-derivation equality test (synthetic contract with castle_brick facade zone; assert top-level facade material_id + base_color_factor match the zone to 3 decimals) and a no-fake-doors test (door-channel atom count equals atoms sampled from mat-6 triangles only).
- [x] Step 2: run — fails (today's top-level facade is the recipe palette + arbitrary HashMap texture).
- [x] Step 3: implement zone→material derivation with deterministic role-default fallback; delete the fake door/stoop blob emitters; keep school flagpole/canopy (they re-anchor in Task 1).
- [x] Step 4: wire in the `run()` loop (line 63 `materials_for` call now passes `generated.asset.as_ref()`); print the `materials: K from zones, F fallback roles` line per asset.
- [x] Step 5: tests green; recook; spot-check `forge.house.victorian.atoms.json` top-level facade is castle_brick (not beige/salmon). *(Recooked victorian facade slot = zone `brick_new`, tint [0.55, 0.28, 0.20] (the castle_brick_02_red texture stem); all 5 assets print `materials: 5 from zones, 3 fallback roles`.)*
- [x] Step 6: leave uncommitted.

## Task 3: Plumb `condition` through the cook (audit item 8: ALL-P1-2 — desaturation path)

**Files:** Modify `src/bin/game_asset_cook.rs` (`DirectiveForge` line 281, `AssetRecipe` line 186, `forge_description` line 2492); tests in `mod tests`.
**Acceptance:** `cargo test --bin game_asset_cook condition -- --nocapture` → prints the emitted description JSON containing `"condition": "Weathered"` for a recipe with that condition, and a zone-passthrough test prints `clapboard_weathered` flowing into `ReadyAssetMaterialZone.material_id` with a tint whose max-min channel spread is measurably smaller than the New tint (printed both).
**Wiring requirement:** `DirectiveForge` gains `#[serde(default = "default_condition")] condition: String` (default `"New"`, validated against New/Aged/Weathered/Derelict — reject otherwise with a `CookError::Directive`); `AssetRecipe` carries it; `forge_description` (line 2499) emits it instead of the hardcoded literal. Forge's `wall_material`/`roof_material` desaturation+roughness then flows into zones → normalized atom tints with zero further plumbing (verified upstream behavior — see IMPORTANT NOTES).

- [x] Step 1: failing tests as above (description emission + synthetic weathered-zone passthrough).
- [x] Step 2: run — fails (`"condition"` is the literal `"New"`; `no field condition on AssetRecipe`).
- [x] Step 3: implement field, default, validation, emission.
- [x] Step 4: wired by construction through `generate_building`'s description payload; directive parse test covers the serde default for old directives.
- [x] Step 5: tests green with printed JSON + tints. *(Printed: emitted description contains `"condition":"Weathered"`; `clapboard_new tint [0.820, 0.750, 0.600] spread 0.220 -> clapboard_weathered tint [0.810, 0.751, 0.623] spread 0.187`; rejection: `test.row has unknown condition 'Rusty': expected one of New, Aged, Weathered, Derelict`. End-to-end: recooked rowhouse v2 payload carries `condition: Aged` and zone ids `stucco_aged`/`slate_roof_aged`/`reveal_stucco_aged`.)*
- [x] Step 6: leave uncommitted.

## Task 4: Multi-variant cooking via RFC-7396 merge-patch (audit item 7: ALL-P0-4 + ALL-P1-13 + ROW-P1-3)

**Files:** Modify `src/bin/game_asset_cook.rs` (directive loading + `starter_recipes` expansion + the `run()` loop), `assets/source/buildings/residential/rowhouse_01.asset.json`; tests in `mod tests`.
**Acceptance:** `cargo test --bin game_asset_cook variant -- --nocapture` → prints a patched directive with `floors: 4`, `condition: "Aged"`, changed seed, and ids `city.res_med.l3.3x4.rowhouse_01`, `….v1`, `….v2`; then `cargo run --bin game_asset_cook` prints **three** `cooked city.res_med.l3.3x4.rowhouse_01*` lines and `pack.json` contains 3 ResidentialMed level-3 assets (assert in a test that reads the written pack via the existing catalog loader).
**Wiring requirement:** `AssetDirective` JSON gains `#[serde(default)] variants: Vec<Value>` of RFC-7396 patches applied to the **raw directive Value** before deserialization (so `forge.seed`, `forge.floors`, `palette`, `condition`, anything patches through one mechanism); variant ids are `{asset_id}.v{n}` (1-based, base asset keeps its id); `merge_patch(target: &mut Value, patch: &Value)` implements RFC 7396 including null-deletes; House-kind starter recipes additionally expand via a small deterministic `starter_variants` (seed-shifted clone each for craftsman and victorian) so res-low pools also grow; variants reuse the base AssemblyPrime export (see IMPORTANT NOTES). Registration in `assets` (run loop line 79) is what grows `zonable_for`'s pool — nothing else needed.

- [x] Step 1: failing tests — `merge_patch` semantics (override, deep merge, null delete) and variant-expansion ids/fields.
- [x] Step 2: run — fails (no such fn/field).
- [x] Step 3: implement merge_patch + expansion in `load_directive_recipes` + `starter_variants`; ship rowhouse_01.asset.json with 2 patches: v1 `{forge:{seed:43017},palette:{accent:[0.42,0.27,0.19]}}` style-shift, v2 `{forge:{seed:43021,floors:4,condition:"Aged"}}` *(condition sits inside `forge` because Task 3 put the field on `DirectiveForge`)*.
- [x] Step 4: wire the run loop so every expanded recipe cooks and registers; per-directive exports reused. *(`exports: HashMap<id, AssemblyPrimeExport>`; id sort puts every base before its `.v{n}`, so 9 cooks ran only 5 `uv` exports.)*
- [x] Step 5: unit tests green; full recook prints the three rowhouse lines + craftsman/victorian `.v1` lines; pack assertion green. *(Printed: `patched v2: floors 3 -> 4, condition New -> Aged, seed 43013 -> 43021`; recook exit 0 with `cooked city.res_med.l3.3x4.rowhouse_01` `.v1` `.v2`, `forge.house.craftsman.v1`, `forge.house.victorian.v1`; pool test picks all three rowhouse ids for seeds 0..3; every variant payload differs byte-wise from its base. NOTE: `src/asset/mod.rs` test `forge_starter_pack_is_the_demo_asset_swap` counts updated 2→4 houses / 5→9 ready assets to match the grown pack.)*
- [x] Step 6: leave uncommitted.

## Task 5: Cook the forge plot — parcel under every building (audit item 3: ALL-P0-5 + ALL-P0-7 cook half)

**Files:** Modify `src/bin/game_asset_cook.rs` (`ForgeRunner::generate_plot`, parcel bake in `atomize_building`), `src/asset/mod.rs` (additive `plot_garden_zones: Vec<ReadyAssetGardenZone>` on `ReadyAssetPayload`, serde-default); tests in cook `mod tests`.
**Acceptance:** `cargo run --bin game_asset_cook` prints per asset `plot: ground N tris, driveway D tris, fence M posts (gate gap G)` with N>20, M>10, G≥2 for `forge.house.craftsman`; `cargo test --bin game_asset_cook plot -- --nocapture` prints fence-break kept/skipped counts against synthetic gate_post sockets and asserts ground atoms span ≈ the recipe footprint centered (printed bounds), catching any frame mismatch.
**Wiring requirement:** `generate_plot(&self, recipe: &AssetRecipe) -> Result<ForgePlotProduct, CookError>` shells forge-cli `plot` with archetype mapped from `CookedKind` (House→`residential_suburban` fence `picket` driveway `concrete`; Rowhouse→`residential_urban` fence `none`; School/Police→`commercial` fence `none` driveway `asphalt`), `footprint: recipe.footprint`, `building_footprint: [forge_width, forge_depth]`, `condition: recipe.condition`, `seed: recipe.seed`. In `atomize_building`, the hardcoded gray pad + stoop atoms (lines 640–656) are REPLACED by the baked parcel: ground/driveway triangles sampled into Ground-channel atoms textured via `TextureCache` (`lawn`/`concrete_pavers`/`asphalt` stems by ground material id: 0 foundation, 1 path, 2 driveway, else lawn); fence instances become Detail-channel post atoms with a gate break (skip posts within 0.8 m of `gate_post_left/right` socket positions, transformed with `forge_to_asset_local`); `garden_zones` pass through to the payload (consumed in Task 8). First implementation step MUST print plot-mesh bounds vs building bounds and reconcile frames before baking.

- [x] Step 1: failing tests — frame/extent assertion + gate-break logic.
- [x] Step 2: run — fails (no `generate_plot`).
- [x] Step 3: implement runner call, frame reconciliation, bake, fence break, passthrough. *(Frames verified in code + live run: plot product is LOT-centered, ground bounds x/z ±footprint/2 at y 0, street side at NEGATIVE z; building asset frame fronts at +z, so reconciliation is a plain z negation (`plot_to_asset_local`). Two contract deltas found vs plan notes: forge-plot's ground returns material id 0 for BOTH foundation and lawn (foundation is detected positionally under the forge footprint), and forge-cli's plot condition matcher is lowercase with a silent Aged fallthrough (the cook lowercases). Gate sockets sit ~3 m beyond the front wall — OUTSIDE small lots (craftsman z 8.25 vs lot edge 6.0) — so gate anchors are clamped onto the lot rectangle before the 0.8 m break test; the real seed-1202 posts flanking the gate land 0.762/0.795 m away and break. Picket/chain-link runs also get connecting rail atoms so the gate gap reads as a real break.)*
- [x] Step 4: wire into `atomize_building` + `ready_payload`; print the `plot:` line in the run loop. *(Also deleted the leftover gray Ground pads in house/police details — superseded by the textured parcel — and stopped `clean_generated_pack_dirs` wiping `textures/`, which had been silently degrading every TextureCache sample to flat tints after each recook because only AssemblyPrime's subset came back mid-cook; `polyhaven_fetch` re-provisioned the pinned sets.)*
- [x] Step 5: tests green; recook shows real parcel counts; `validate_quality` Ground-channel minimum updated so a missing plot fails the cook. *(Printed: `fence-break: kept 4 posts, skipped 2 at the gate`; `plot ground: 192 atoms from 32 tris, bounds x [-5.66, 5.68] z [-5.68, 5.66] vs lot 12x12 centered`; `plot driveway: 6 atoms from 2 tris, first at [1.89, 0.03, 5.87] (front wall z 5.25)`; missing-parcel rejection `0 Ground-channel atoms, expected at least 40`. Recook: `forge.house.craftsman plot: ground 32 tris, driveway 2 tris, fence 21 posts (gate gap 2)` — N=32>20, M=21>10, G=2≥2 ✓; all 9 assets print `plot:` lines (rowhouse/school/police cook fence-less urban/commercial parcels per the kind mapping: ground 18 tris, driveway 2 tris). Cooked craftsman payload carries `plot_garden_zones` `[flower_bed @ (-0.30, 4.50), shrub @ (4.00, 0.24)]` and 55 texture-sampled lawn atoms (27 distinct colors, mean ≈ authored tint). Full `cargo test` 243 passed / 0 failed.)*
- [x] Step 6: leave uncommitted.

## Task 6: Consume cooked sockets at placement — path, frontage, chimney (audit item 4: ALL-P0-6)

**Files:** Modify `src/render_gpu/mod.rs`; tests in its `mod tests`.
**Acceptance:** `cargo test --lib render_gpu socket -- --nocapture` → loads the real cooked craftsman payload from `assets/buildings/forge_starter` (same pattern as `asset/mod.rs` test line 951), places it, and prints path endpoints + count: every path splat within 0.5 m of the segment `path_start` → true door, count ≥ 6; a frontage test prints the corrected yaw for a payload whose `road_frontage` forward disagrees with +Z; a chimney test asserts ≥2 splats centered within 0.3 m of `chimney_top` (House kinds only).
**Wiring requirement:** implemented inside `place_ready_asset_splats` (and mirrored in the mesh path via `place_ready_asset_detail_meshes`) so every placement gets it: (a) paver path strip (Ground channel, ~0.7 m wide, ≤0.5 m sample spacing) from the `path_start` socket to the **true door** located from mat-6 mesh triangles with the `door_front` socket normal as the outward disambiguator (replicate `door_anchor`, forge_pathtrace.rs:477 — read-only reference, no import); (b) `road_frontage` snap-point forward used to apply a deterministic correction yaw so the frontage faces the lot's +Z road edge; (c) `chimney_top` seats a 2-splat chimney cap + a 3–4 splat faint smoke wisp column, present deterministically per `instance_seed` (no sim coupling — `src/game/**` untouched). Gate-post fence breaks already happened at cook (Task 5).

- [x] Step 1: failing tests as above (real payload, real geometry assertions).
- [x] Step 2: run — fails (zero socket readers in render_gpu today — verified; 18 compile errors, no `InstanceFrame`/`door_path_splats`/`frontage_correction_yaw`/`chimney_cap_splats`).
- [x] Step 3: implement socket lookup helpers + the three consumers, all transformed through the instance frame (position/yaw/mirror-aware so Task 7 composes).
- [x] Step 4: wired by construction inside the placement fns both call sites (lots line ~750, services line ~854) already use. *(Mesh path mirrors the consumers: `place_ready_asset_detail_meshes` now returns the socket splats, which `place_ready_asset_mesh_renderables` hands back as its detail splats. Services pass `service_instance_seed(b.position)`.)*
- [x] Step 5: tests green with printed endpoints/yaw/counts. *(Printed: `path: 8 splats from [28.60, 0.05, -1.65] to [26.81, 0.05, -2.76], max segment distance 0.000 m, true door local x +0.79 (door socket x 0.00)`; `frontage: forward [1,0,0] -> correction -90.0 deg; corrected forward within 2.71 deg of the road (yaw jitter -2.71 deg)`; `chimney: 2 cap splats within 0.3 m of chimney_top [1.42, 6.5, -1.57]; school consumers = door path only (7 splats)`. NOTE: this cargo rejects two positional filters, so the acceptance command runs as `cargo test --lib render_gpu::tests::socket -- --nocapture` — same tests.)*
- [x] Step 6: leave uncommitted.

## Task 7: Per-instance variation off lot.seed (audit item 2: ALL-P0-3 + ALL-P1-14)

**Files:** Modify `src/render_gpu/mod.rs`; tests in its `mod tests`.
**Acceptance:** `cargo test --lib render_gpu variation -- --nocapture` → prints, for the cooked craftsman placed with seeds 1 and 2: fraction of atom x-positions that differ (≥0.30 — mirror fired on one), mean per-channel |Δcolor| on Facade/Trim atoms (>0.005), setback delta in 0.3–0.8 m, yaw delta ≤3°; and seed 1 placed twice is **bit-identical** (memcmp over serialized splats).
**Wiring requirement:** `fn instance_variation(seed: u64) -> InstanceVariation` (splitmix64-style mixing; fields: `mirror_x: bool` ~50 %, `yaw_jitter` ±1.5–3°, `setback_jitter` 0.3–0.8 m along local Z, facade/trim HSV jitter h ±0.02 s ±0.08 v ±0.06, anti-CGA nonuniform scale ±0.8 % — forge's uncompiled `stochastic.rs` idea, reimplemented game-side) applied in `place_ready_asset_splats`, `place_ready_asset_mesh_renderables` (mirror = negate vertex x + swap two indices to preserve winding), `place_ready_asset_detail_meshes`, and the Task 6 socket consumers via one shared instance-frame helper. `place_ready_asset_sdf_instance` receives position/yaw/setback but NOT mirror (see IMPORTANT NOTES). Callers updated: lot loop passes `l.seed`; service loop passes a hash of `b.position` bits. Small in-file `rgb↔hsv` helpers (no deps).

- [x] Step 1: failing tests as above (difference + determinism, real cooked payload).
- [x] Step 2: run — fails (placement was translation+yaw only; red = no `variation_atom_rgb` color path and the 5-arg `place_ready_asset_sdf_instance`). *(`instance_variation` + `InstanceFrame` themselves landed in Task 6 because the socket consumers must ride the same frame; Task 7's red was the color-jitter + SDF wiring.)*
- [x] Step 3: implement variation struct, hash, HSV jitter, mirror for splat/mesh/detail paths + shared frame helper. *(Mirror on meshes = negate x + swap two indices via `InstanceFrame::local_triangle`; in-file `rgb_to_hsv`/`hsv_to_rgb`; anti-CGA scale rides `apply_transform`'s scale argument.)*
- [x] Step 4: wire both call sites and the SDF instance (sans mirror); socket consumers from Task 6 go through the same frame. *(Lots pass `l.seed`; services pass `service_instance_seed(b.position)` = splitmix of the position bits.)*
- [x] Step 5: tests green with printed measured deltas; `cargo run --release --bin city_coverage` → `play_houses_front_closeup.png` shows the two adjacent craftsmans visibly different. *(Printed: `variation seeds 1 vs 2: x-shift fraction 0.98, mean |dcolor| [0.0044, 0.0180, 0.0015] (all 0.0080), setback 0.66 vs 0.54 m (delta 0.12), yaw +2.88 vs +1.71 deg, mirror false vs true`; `determinism: seed 1 placed twice -> 9412 splats, 611780 serialized bytes, bit-identical`; `sdf: rotation_y 0.4503 rad = yaw 0.4 +0.0503 jitter (+2.88 deg), setback 0.66 m, volume untouched (no mirror)`. The acceptance text's "setback delta in 0.3–0.8 m" is read as each instance's setback inside the authored 0.3–0.8 band (delta of two such draws cannot itself be bounded there); both setbacks and their delta are printed. `city_coverage` re-rendered all 4 closeups; the adjacent craftsmans now differ — mirrored door/path, planter and mailbox sides swapped. Same cargo filter caveat as Task 6: run as `cargo test --lib render_gpu::tests::variation -- --nocapture`.)*
- [x] Step 6: leave uncommitted.

## Task 8: Cook forge vegetation + fill yards and gardens (audit item 9: ALL-P1-16 + ALL-P1-17)

**Files:** Modify `src/bin/game_asset_cook.rs` (`ForgeRunner::generate_vegetation`, flora atomizer + registration), `src/asset/mod.rs` (additive `flora` section on `AssetPack` + catalog accessor, serde-default), `src/render_gpu/mod.rs` (resolver + yard/garden instancing); tests in both `mod tests`.
**Acceptance:** `cargo run --bin game_asset_cook` prints `flora: oak_tree 8.0m -> A atoms, birch_tree -> B, shrub -> C, tall_grass -> D` (all non-zero) and writes `atoms/veg.*.atoms.json`; `cargo test --lib render_gpu yard -- --nocapture` → a developed res-low lot scene contains ≥ 40 Vegetation-channel splats whose XZ positions are OUTSIDE the building footprint and inside the lot (printed count + sample positions); same-seed determinism asserted.
**Wiring requirement:** cook: `generate_vegetation` shells forge-cli `vegetation` for `oak_tree`, `birch_tree`, `shrub`, `tall_grass` (fixed seeds), atomizes the returned mesh into Vegetation-channel atoms (first step prints the mesh's material-id histogram to map trunk vs leaf colors — do not guess), writes `veg.{species}.atoms.json` payloads and registers them in the pack's new `flora` list (NOT as `BuildingAsset`s — they must not enter zonable pools or build-tool lists). game: a fixed resolver table `garden asset_query → flora id + scale` (`shrub→veg.shrub`, `flower_bed→veg.shrub @0.5 + tint`, `wildflower|herb_garden→veg.tall_grass`, default lawn-skip) consumed at placement: 1–2 yard trees per developed residential lot (deterministic positions from `instance_seed`, rejected if inside building bounds) + each payload `plot_garden_zones` ring-filled with `N = density × area` capped instances, splitmix-positioned within the zone radius. All instancing inside `place_ready_asset_splats`'s flow so mirror/yaw from Task 7 applies.

- [x] Step 1: failing tests — cook-side flora payload shape (≥50 atoms, Vegetation channel, height ≈ requested) and game-side yard-fill count/containment.
- [x] Step 2: run — fails (zero forge-vegetation references in the game today — verified; 19 cook + 15 render compile errors, no `atomize_vegetation`/`vegetation_splats`/`resolve_garden_flora`).
- [x] Step 3: implement runner call, histogram print, atomizer, flora registration, resolver, yard/garden fill. *(Histogram verified live: vegetation meshes carry trunk/branch cylinders on material id 0 and leaf billboards on id 2 — `flora oak_tree mesh: 16692 tris, material histogram {0: 15936, 2: 756}`. The atomizer voxel-clusters triangle centroids per class (BTreeMap ⇒ byte-stable output), marks leaves with opacity 235 (the runtime bloom-tint hook) and grounds the cloud — centroid clustering floats the lowest cylinder ring ~0.16 m, so the cook seats the base on y=0. Yard trees: the full mesh AABB swallows roof overhang + porch (craftsman mesh z reaches 8.7 on a ±6 lot), so the building rect is the WALL skin (mat-0) AABB and trees plant at the 4 lot-corner insets that clear it — guaranteed-outside anchors instead of blind rejection sampling, because the tight starter lots leave only ~1.25 m side strips. Garden fill rejects samples inside the walls or off the lot (forge's flower_bed at z 4.5 sits INSIDE the craftsman wall rect — those samples skip).)*
- [x] Step 4: wire: cook loop prints the `flora:` line; catalog loads flora payloads (`AssetPack.flora: Vec<FloraAsset>` serde-default + `flora_assets()` accessor, payloads in the same `ready_asset` store but invisible to `get`/`zonables_for`/`ploppables_of`); placement instances them in BOTH paths (`place_ready_asset_splats` and the mesh path's detail-splat return), after the socket consumers, all through the shared `InstanceFrame`.
- [x] Step 5: tests green; recook + `city_coverage` → `play_houses_closeup.png` shows yard trees and garden clusters. *(Printed: `flora: oak_tree 8.0m -> 1711 atoms, birch_tree 7.0m -> 978 atoms, shrub 1.2m -> 147 atoms, tall_grass 0.6m -> 8 atoms` — 4 cooked species, `atoms/veg.*.atoms.json` written + registered in `pack.json` `flora` (not `assets`); `yard: 4892 vegetation splats (3646 outside building footprint x [-4.75, 4.75] z [-5.29, 5.29], inside the 6x6 half-lot), 4 tree anchors`; `yard determinism: seed 9 grew 4892 splats twice (317980 bytes, bit-identical); seed 10 grew 2007 splats (differs)`. Full `cargo test` 252 lib + 28 cook-bin, 0 failed; forge `cargo test -p forge-building -p forge-vegetation` 78 + 5 passed (no forge edits). `play_houses_closeup.png`/`play_houses_front_closeup.png` re-rendered: corner yard trees between lots, bloom-tinted flower beds at the porches. Same cargo filter caveat as Tasks 6–7: run as `cargo test --lib render_gpu::tests::yard -- --nocapture`.)*
- [x] Step 6: leave uncommitted.

---

## Final verification (after Task 8)

- [x] Run the **Done When** command pair; eyeball the four `play_*_closeup.png` renders against the audit's named defects (identical twins, bare green, floating rooftops, fake doors). *(Final verifier 2026-06-10: recook printed all 9 `cooked …` lines incl. rowhouse `.v1`/`.v2`, per-asset `plot:` lines — craftsman `ground 32 tris, driveway 2 tris, fence 21 posts (gate gap 2)` — and `flora: oak_tree 8.0m -> 1711 atoms, birch_tree 7.0m -> 978, shrub 1.2m -> 147, tall_grass 0.6m -> 8`. Python payload audit: every flat-roof kind has 63–99 rooftop atoms seated ≤0.15 m of mesh roof_top, worst floating bottom +0.128 m (gate 0.25); Ground atoms 198–690/asset; rowhouse v2 zones `stucco_aged`/`slate_roof_aged`; pool of 3 at l3; 4 flora payloads grounded at y 0. Before/after `city_coverage` captures (baseline = variation+vegetation neutralized via temporary env-gated switch, removed after capture): before shows bit-identical craftsman twins on bare lawn; after shows mirrored/setback-jittered neighbors, angled paver paths, bloom-tinted flower beds at the porches, yard trees between lots; school/police roofs carry seated RTU boxes + bulkhead + vent stacks.)*
- [ ] Read-only Spectra pass: run the `forge_pathtrace` env recipe from IMPORTANT NOTES; record the printed per-view means and the `gates:` line. If a band trips purely from legitimate visual change (e.g. school roof no longer bare slab), hand the numbers to the porch-closure owner — do **not** edit the harness. *(DEFERRED by the orchestrator: forge_pathtrace is owned by the porch-closure agent; visual verification through that binary is explicitly deferred. All other verification ran through city_coverage instead.)*
- [x] `cd ~/Ochroma/projects/urban_horizon && cargo test` green; `cd ~/src/forge && cargo test -p forge-building` green (and `-p forge-plot -p forge-vegetation` if contingency edits happened). *(Fresh final-verifier runs: game lib `252 passed; 0 failed`, game_asset_cook bin `28 passed; 0 failed`, full `cargo test` exit 0; forge `cargo test -p forge-building` `78 passed; 0 failed`; forge-cli `--test plugin_contract` `3 passed; 0 failed`. No forge contingency edits were made.)*
- [x] Leave everything uncommitted; summarize per-repo diffs for the user. *(No commits anywhere; forge untouched; temp baseline switch in render_gpu/mod.rs fully reverted — grep count 0.)*

## Self-Review Checklist

- [x] Every task implements AND wires in the same task — no "wire later" tasks exist
- [x] Every `Acceptance` criterion names a real non-trivial expected output (printed seat gaps, tint triples, kept/skipped counts, pixel-visible PNGs — never "tests pass")
- [x] Every `Wiring requirement` names an exact function and exact file (with today's verified line numbers)
- [x] `IMPORTANT NOTES` contains real, code-verified API signatures (placement fns, ReadyAssetAtom/Socket/Zone, ForgeRunner contract, plot/vegetation CLI params, condition semantics)
- [x] `File Map` lists every file that appears in any task
- [x] No step contains `todo!()`, `unimplemented!()`, or stub bodies
- [x] `Done When` names a specific command pair and a specific human-observable result (named PNGs with named visible changes + exact printed cook lines)
- [x] All types, method names, and signatures are consistent across all tasks (instance frame shared by Tasks 6–8; zone derivation shared by Tasks 2–3; merge-patch feeds Tasks 3–4)
- [x] Ownership boundaries respected: no `src/game/**`, no `forge_pathtrace.rs` edits, `src/asset/mod.rs` additive-only, forge contingency-only with no git
