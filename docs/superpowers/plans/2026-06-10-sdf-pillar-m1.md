# SDF Pillar — M1 Wave Plan (GPU Cook, snorm8 Atlas, Clearance, Childcare Field, Volumetric Fix, Spectra Probe)

> **For agentic workers:** REQUIRED SUB-SKILL: Use **superpowers:subagent-driven-development** (recommended) or **superpowers:executing-plans** to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the design's cheapest-payoff-first wave of the SDF pillar: the canonical `vox_core::sdf` types plus a GPU mesh→SDF cook (scatter-min + JFA + generalized-winding-number sign, gated against a CPU GWN oracle), the Tier-1 snorm8 GPU atlas with its 64 MB quota proven at 200 unique assets, the already-written-but-unwired placement-clearance queries wired into the game's `place_at`, a Tier-3 masked-JFA 2D field replacing Euclidean circles for childcare with a before/after overlay in `play`, the `VolumetricPass` wrong-entry-point latent bug fixed with a construction regression test, and Spectra's upload-only `SdfLayer` given its first real GPU consumer (decide-and-act: **keep + wire**, per design §4.10).
**Done When:** All four, each human-observable:
1. *(Group C)* `cd ~/Ochroma/projects/urban_horizon && cargo run --release --bin game_asset_cook` prints, per starter asset, a line shaped `[sdf] forge.house.craftsman 64x56x51 vox=0.22m band=±0.89m <K> KB sign=gwn closed=yes cook=<ms>ms (gpu)` (resolution axes > 24, voxel < 0.30 m) and a final `[sdf-validate] 5/5 inside-negative OK | |grad|-1 p95 <p> | open-mesh 0` with p < 0.15, exit 0; the holed-mesh test makes the bake return `OpenMesh` with the fractional |w| printed.
2. *(Group A)* In `cargo run --release --bin play`, hovering+clicking a care ploppable onto a developed building shows the status-bar centre text `blocked: clearance <c> m` (c < 1.0, measured) and refuses placement; clicking 3 m away places normally.
3. *(Group D)* In `play` on a river map: key `c` toggles the childcare coverage overlay, key `p` writes `play_coverage_<NN>.png`; the before PNG shows far-bank homes red, then after plopping a childcare across the river + one Space tick the after PNG shows them green and the info window line `Childcare reach <R>%` increased.
4. *(Groups A/B)* `cargo test -p vox_render --lib sdf_ -- --nocapture` prints `[sdf_bake] center=<−> corner=<+>` and `[sdf_atlas] 200 assets requested | resident=<≈36> MB (quota 64) | evicted=0`; `cargo test -p vox_render --lib df2d -- --nocapture` prints `[df2d] field=<g> m euclidean=<e> m` with g > 3·e; `cargo test -p vox_render --lib volumetric -- --nocapture` prints `[volumetric] luma mie0.01=<a> mie0.2=<b>` with b < a; the spectra probe test prints `[sdf_probe] 64 probes max |Δd| = <d> m (gpu vs cpu)` with d < half a voxel.

**Architecture:** Three-tier distance-field stack per the design, M1 slice only: Tier-1 per-asset dense snorm8 fields cooked once on GPU (`SdfBaker`, a WGSL port-and-upgrade of spectra's proven `physics_sdf_gen.slang` three-pass structure with the parity sign replaced by generalized winding number) resident in one `R8Snorm` 3D atlas with hardware trilinear filtering; Tier-3 city-scale 2D masked-JFA fields with integer-deterministic seed propagation; consumers wired this wave are placement clearance (CPU `SpatialFieldRegistry`/`SdfColliderSet`, already shipped and tested) and childcare coverage. Tier-2 clipmap, AO, imposters, destruction are explicitly later waves.
**Design Document:** `docs/superpowers/specs/2026-06-10-sdf-pillar-design.md`
**Tech Stack:** Rust (workspace toolchains as-is), wgpu 24 + WGSL (vox_render; **no Slang, no SPECTRA env** for engine tasks), glam 0.29, bytemuck 1, tiny-skia 0.11 (civitas PNG), Spectra Slang→SPIR-V via `spectra-gpu` `vulkan-backend` feature (Task 2 only).
**Build:** per-repo `cargo test` with **targeted filters only** (see flake note); GPU verification on the AMD 780M (RADV); Task 2 needs `LD_LIBRARY_PATH=$HOME/slang-sdk/lib VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json`.

---

## EXECUTION GATES — ownership locks (other workflows are executing RIGHT NOW)

Tasks are grouped by the lock they wait on. **A group may start the moment its lock owner releases; groups are independent unless a task names a cross-group dependency.**

| Group | Gate | Tasks | Locked surface |
|---|---|---|---|
| **A** | **NONE — start immediately** | 1, 2 | civitas `src/spatial_field.rs` + `src/bin/play.rs` (Task 1); spectra `slang/` + `rust/spectra-scene-upload/` (Task 2) |
| **B** | **EXECUTE ONLY AFTER `engine-virtualized-m1m2` workflow completes** (vox_render owned by the virtualized-rendering workflow: `atom_instances.rs`, splat renderer, `lib.rs`/`gpu/mod.rs` registrations) | 3, 4, 5, 6 | everything under `~/src/ochroma/crates/vox_render/` incl. `sdf_scene.rs`, `gpu/volumetric_pass.rs`, `gpu/mod.rs` |
| **C** | **EXECUTE ONLY AFTER `assets-remediation` workflow completes AND Task 3 has landed** (assets-remediation owns `game_asset_cook.rs` + `render_gpu/`) | 7 | civitas `src/bin/game_asset_cook.rs`, `src/asset/mod.rs` |
| **D** | **EXECUTE ONLY AFTER `living-instances` workflow completes AND Task 5 has landed** (living-instances owns `src/game/`, `src/lot/`) | 8 | civitas `src/game/mod.rs` (plus unowned `coverage_field.rs`, `game_mechanics/care/mod.rs`, `play.rs`) |

Additional ownership rules:
- `~/Ochroma/projects/urban_horizon/src/bin/forge_pathtrace.rs` is owned by the **porch agent** — this plan never touches it; Task 2 also avoids `~/src/spectra/rust/spectra-renderer/src/renderer.rs` (porch agent's first-frame fix lives there).
- **Calling** locked code is allowed everywhere (e.g. `render_gpu::gpu_context()`, `render_gpu::resident_sdf_registry()`); **editing** it is what the gates forbid.
- `game_asset_cook.rs` is being edited right now — **all line numbers drift; locate code by function name, never by line.**

---

## IMPORTANT NOTES

- **Repos:** engine `~/src/ochroma` (branch `blitz/day1-foundation`, uncommitted work), game `~/Ochroma/projects/urban_horizon` (master, uncommitted), `~/src/spectra` (git, uncommitted), `~/src/forge` (**NOT a git repo** — and this plan does NOT edit forge at all). **Do NOT run `git commit` anywhere** — the user commits. Template commit steps are replaced by "leave uncommitted".
- **vox_render test discipline:** never require a full `cargo test -p vox_render --lib` run — the pre-existing `resident_gi_seam` radix_sort raster-nondeterminism flake (documented in the virtualized plan and the resident-frame memory) fails full runs. Every acceptance below is a **targeted filter**. GPU tests use the house pattern: in-module `#[cfg(test)]`, a `try_gpu_context(label)` helper (copy the shape from `spectral_gi.rs::try_gpu_context`, ~line 1303) that requests an adapter, calls `crate::gpu::adapter::ensure_hardware(&info)`, prints `[<label>] no adapter — skipping GPU test` / the software-adapter skip line and returns `None` so GPU-less CI stays green.
- **Verified existing signatures (code-checked 2026-06-10; where the design doc disagreed, these win):**
  - `vox_physics::sdf` (UNLOCKED crate): `SdfVolumeDesc { pub resolution: [u32;3], pub origin: Vec3, pub voxel_size: f32, pub narrow_band: f32 }`; `SdfVolume::from_f32_grid(desc, Vec<f32>) -> Result<Self, SdfVolumeError>`, `from_snorm16_grid(desc, &[i16]) -> Result<Self, SdfVolumeError>`, `sample_local(Vec3) -> Option<f32>`, `normal_local`, `distances() -> &[f32]`, `desc()`, `local_bounds()`; `SdfColliderSet::{add_instance(SdfAssetId, Arc<SdfVolume>, Vec3, Quat, f32) -> SdfInstanceId, sample_world(Vec3) -> Option<SdfSample>, normal_world, project_point(Vec3, clearance: f32) -> Option<SdfContact>, raycast_sdf(origin, dir, max_distance, hit_epsilon) -> Option<SdfRayHit>}`; `SdfContact { instance_id, asset_id, point, projected_point, normal, signed_distance, penetration }` (all pub); trait `SdfQuery` mirrors these. 7 in-module tests exist — keep green.
  - civitas `spatial_field.rs` (UNLOCKED): `SpatialFieldRegistry::{from_instances(Vec<SpatialSdfInstance>), add, sample_signed_distance([f32;3]) -> Option<SpatialDistanceHit>, is_solid([f32;3], clearance: f32) -> bool, to_physics_colliders() -> Result<SdfColliderSet, SdfVolumeError>}`; `SpatialDistanceHit { instance_index, asset_id: String, distance: f32, local_position }`; `stable_sdf_asset_id(&str) -> SdfAssetId` (FNV); `ready_asset_sdf_desc(&ReadyAssetSdfVolume) -> SdfVolumeDesc`. Test helper `cube_sdf()` in its tests is the model for new test volumes.
  - civitas `asset::ReadyAssetSdfVolume { pub resolution: [u16;3], pub origin: [f32;3], pub voxel_size: f32, pub narrow_band: f32, pub distances_snorm16: Vec<i16> }` + `decode_distance(i16) -> f32`, `is_well_formed()`, `sample_count()`.
  - civitas cook (LOCKED until Group C): `fn ready_sdf(mesh: &ReadyAssetMesh) -> Option<ReadyAssetSdfVolume>` — CPU O(V·T) point-triangle + `point_inside_mesh` single-parity-ray sign; `SDF_TARGET_MAX_RES: u16 = 24`, `SDF_PADDING_METRES: f32 = 1.0`, `voxel_size = (longest/(24-1)).max(0.20)`, `narrow_band = (voxel*8).max(1.0)`, `encode_sdf_distance(d, band) -> i16`. Called once per recipe in `run()` (`let sdf = ready_sdf(&ready_mesh);`). Existing test `ready_sdf_marks_closed_mesh_inside_negative` (closed 2×2×2 cube, center < 0, padded corner > 0) — keep and extend.
  - civitas `play.rs` (UNLOCKED): `GameView { game: CivitasGame, render_scene: render_gpu::CityRenderScene, scene: SceneRenderer, stats: CareStats, observer, info_open, … }`; `fn place_at(&mut self, px, py, w, h) -> bool` calls `render_gpu::screen_to_ground` then `self.game.try_place_care(k, pos)` / `try_place_city_service`; `refresh_visible_atoms` rebuilds `self.render_scene` via `render_gpu::city_render_scene_for_camera` (camera-filtered — its `sdfs` cover what is on screen); `fn render(...) -> Result<Vec<[u8;4]>, String>` builds the status bar with a `center: String` (Day/time or zoning prompt) passed to `hud::draw_bottom_bar_ui`; `fn info_lines(&self) -> Vec<String>`; `fn key(&mut self, key: &Key) -> bool` has free letters `c` and `p`. PNG writing pattern: `tiny_skia::Pixmap` + `PremultipliedColorU8::from_rgba` exactly as `city_coverage.rs::save_png`.
  - civitas `render_gpu` (LOCKED for edits, callable): `pub fn gpu_context() -> Result<GpuContext, String>` (OnceLock, headless); `resident_sdf_registry(&CityRenderScene) -> SpatialFieldRegistry`; `CityRenderScene { pub sdfs: Vec<ResidentSdfVolume>, … }` where `ResidentSdfVolume = SpatialSdfInstance`.
  - civitas game (LOCKED until Group D): `CivitasGame { pub terrain: MapTerrain, … }`; `try_place_care(&mut self, CareKind, [f32;2]) -> Result<u32, String>`; tick calls `self.care.assign_slots_with_barrier(&self.demographics, |bp, dp| terrain.line_crosses_water(bp, dp))` (find by function name); `MapTerrain::{is_buildable(wx,wz), is_water(wx,wz), sample_y(wx,wz) -> f32, is_flat(), line_crosses_water([f32;2],[f32;2]) -> bool}`.
  - civitas care (UNLOCKED): `CareParams { coverage_radius_m: f32, … }`; `CareKind::{Kindergarten, Daycare}` serve `DependentKind::Infant` (= "childcare"); `assign_slots_with_barrier(&mut self, demo: &Demographics, blocked: impl Fn([f32;2],[f32;2]) -> bool) -> CareCoverage` — **the Euclidean circle test (`d2 > r*r`) lives HERE in `care/mod.rs`, not in `service.rs` (design's wiring table is off by a file: `service.rs:47` is only the field declaration)**; `CareCoverage::{coverage_ratio(stage) -> f32, covered: HashSet, …}`.
  - `vox_render::gpu::GpuContext` (`gpu/mod.rs`): `#[derive(Clone)]`; `from_parts(&wgpu::Device, &wgpu::Queue, &wgpu::AdapterInfo) -> Self`; `device()`, `queue()`, `adapter_name()`. Shared-context constructor precedent: `GpuGi::new_with_context(ctx, …) -> Self`.
  - `vox_render::sdf_scene` (LOCKED until Group B; this plan does not modify it, listed for context): `RenderSdfScene::from_collider_set(&SdfColliderSet) -> Self`, `to_spectra_sdf_layer()` (feature `spectra-native`), `GpuSdfVolumeHeader`/`GpuSdfInstanceHeader` Pod structs.
  - `VolumetricPass` (`gpu/volumetric_pass.rs`, LOCKED until Group B): **the verified latent bug** — the resolve pipeline is built from `scatter_compute.wgsl` with `entry_point: Some("scatter_compute")` against `resolve_bgl` whose bindings 1/2/3 (storage-ro / storage-texture Rgba32Float / depth-texture) do NOT match the shader's declared bindings 1/2/3 (uniform / storage-rw / storage-ro) → `create_compute_pipeline` fails wgpu validation on any real device. Latent because `VolumetricPass` has **zero constructors anywhere in the workspace** (grep-verified). Also: `scatter_compute.wgsl::sample_sdf_at` is hard-coded to `dims=(1,1,1)` defaults, so scatter binding 3 contributes nothing real yet; its first defined producer (the Tier-2 clipmap) is a later wave. `dispatch_resolve(&self, encoder, device, camera_buf, splat_output_texture: &wgpu::Texture /*Rgba32Float*/, depth_texture: &wgpu::Texture)`.
  - spectra (Task 2): `GpuBackend::{upload_f32(&[f32]) -> Result<GpuBufferHandle, GpuError>, download_f32(&GpuBufferHandle, &mut [f32]), compile_kernel(slang_path: &Path, entry_point: &str, include_dirs: &[&Path]) -> Result<CompiledKernelId, GpuError>, dispatch(CompiledKernelId, thread_count: [u32;3] /*threads, NOT groups*/, &BindingMap)}`; `BindingMap::bind_buffer(name, handle)`; `spectra_gpu::VulkanSlangBackend::new(ordinal: usize) -> GpuResult<Self>` behind feature `vulkan-backend`. `Uploader::new()`, `upload_from_state(&SceneState, None, &mut gpu, None) -> UploadResult<(GpuScene, UploadStats)>`; `GpuScene` pub fields `sdf_distances/sdf_volume_headers/sdf_instance_headers: Option<GpuBufferHandle>`; the binding-name map in `gpu_scene.rs` binds them as `g_sdf_distances` / `g_sdf_volume_headers` / `g_sdf_instance_headers` — **grep-verified: NO .slang file declares these today (the dead end this plan ends)**. Header float layouts (from `pack_sdf_volume_headers`/`pack_sdf_instance_headers`): volume = 12 floats `[asset_id, res.xyz, origin.xyz, voxel_size, narrow_band, distance_offset, distance_count, 0]`; instance = 19 floats `[instance_id, asset_id, volume_index, position.xyz, rotation_xyzw, uniform_scale, aabb_min.xyz, aabb_max.xyz, 0, 0]`. `spectra_scene_state::{SceneState::new(w,h), SdfLayer::from_parts(volumes, instances, distances), SceneState.sdf, mark_sdf_changed()}`. Reference algorithm for Task 3: `~/src/spectra/slang/physics_sdf_gen.slang` (3 passes: `seed_sdf` one-thread-per-triangle Ericson closest-point into ±2-voxel AABB with race-y min, `jfa_pass` 26-neighbor carrying closest points, sign pass; **cube-res only `u_resolution`** — the WGSL port generalizes to `[nx,ny,nz]`).
  - forge: `generalized_winding_number(mesh: &Mesh, q: DVec3) -> f64` and `orient_outward(&mut Mesh)` exist in `~/src/forge/crates/building/src/lib.rs` but are **private fns — they cannot be imported. Task 3 re-implements GWN in `vox_core::sdf` (sum of `triangle_solid_angle` / 4π, f64 accumulation) so cook sign and forge winding repair share the same mathematical authority, not the same symbol.**
- **WGSL/wgpu pins (Group B):** storage atomics are u32/i32 only — the scatter-min seed uses `atomicMin` on the **ordered-bits encoding of non-negative f32** (bitcast is monotonic for d ≥ 0), then an equality-guarded second triangle pass writes the closest-point payload (benign race among equal distances, exactly the race the Slang source documents). `R8Snorm` 3D textures are core-filterable in wgpu 24 (sample returns [-1,1]; decode = `v * narrow_band`). New module registrations go in `crates/vox_render/src/gpu/mod.rs` (`pub mod sdf_bake; pub mod sdf_atlas; pub mod distance_field_2d;`) — that file is inside the Group-B lock.
- **Determinism pin (Tasks 5/8):** the 2D JFA carries seed cell coords in `rg16uint` and compares **integer squared cell distances** — bit-deterministic across GPUs; the sim consumes distances recomputed CPU-side from the read-back seed grid (f16 distance texture is render/debug output only). Coverage feeds employment gating, so this is a correctness requirement, not a nicety.
- **New API contracts (implement EXACTLY; design §5/§6 reconciled with code):** `SdfDesc`/`SdfField`/`SdfPayload::{Snorm8(Vec<i8>), Snorm16(Vec<i16>)}`/`SdfSign::{Closed, Shell}`/`SdfValidation` with **private fields + accessors** as written in design §5; `SdfBaker::{new_with_context(&GpuContext) -> Result<Self, SdfBakeError>, bake(&mut self, positions: &[[f32;3]], indices: &[[u32;3]], target: SdfBakeTarget) -> Result<(SdfField, SdfValidation), SdfBakeError>}` with `SdfBakeTarget { max_axis_res: u32, min_voxel: f32, pad: f32, band_voxels: f32 }` and `SdfBakeError::{DegenerateMesh, OpenMesh { min_abs_winding: f32 }, DeviceLimits(String)}`; `SdfAtlasGpu::{new_with_context(&GpuContext, quota_bytes: u64) -> Result<Self, SdfAtlasError>, insert(&mut self, SdfAssetId, &SdfField) -> Result<u32, SdfAtlasError>, resident_bytes() -> u64, texture(), header_buf()}` + `GpuSdfFieldHeader` (48 B Pod, design §5); `DistanceTransform2d::{new_with_context(&GpuContext, size: u32) -> Result<Self, Df2dError>, encode_from_cells(&mut self, &GpuContext, mask_cells: &[u8], seeds: &[[u32;2]]) /*uploads mask, encodes, submits — cook/sim-paced, not frame-coupled*/, distance_texture(), seed_id_texture(), read_back(&self, &GpuContext) -> Df2dReadback}` where `Df2dReadback` exposes `seed_id(x,y) -> Option<u16>` and `distance_m(x,y, cell_size_m) -> Option<f32>` (CPU-recomputed from seed coords).
- `todo!()` / `unimplemented!()` / empty function bodies are **forbidden** — they fail the task. Every test asserts a real computed value and prints it.

---

## File Map

| Action | Path | Gate | Responsibility |
|--------|------|------|----------------|
| Modify | `~/Ochroma/projects/urban_horizon/src/spatial_field.rs` | A | `placement_blocked` multi-probe clearance helper + lib tests |
| Modify | `~/Ochroma/projects/urban_horizon/src/bin/play.rs` | A (Task 1) + D (Task 8) | clearance gate in `place_at` + `blocked:` status text; coverage overlay, screenshot key, HUD reach line |
| Create | `~/src/spectra/slang/sdf_probe.slang` | A | `sample_scene_sdf` + `probe_sdf` kernel — first reader of `g_sdf_*` buffers |
| Create | `~/src/spectra/rust/spectra-scene-upload/tests/sdf_probe_gpu.rs` | A | GPU-vs-CPU SdfLayer sampling parity test |
| Modify | `~/src/spectra/rust/spectra-scene-upload/Cargo.toml` | A | dev-dep `spectra-gpu` with `vulkan-backend` |
| Create | `~/src/ochroma/crates/vox_core/src/sdf.rs` | B | canonical `SdfDesc`/`SdfField`/`SdfValidation`, eikonal validator, CPU GWN oracle |
| Modify | `~/src/ochroma/crates/vox_core/src/lib.rs` | B | register `pub mod sdf;` |
| Modify | `~/src/ochroma/crates/vox_physics/src/sdf.rs` | B | additive `pub use vox_core::sdf::{…}` shim (SdfVolume untouched) |
| Create | `~/src/ochroma/crates/vox_render/src/gpu/sdf_bake.rs` + `sdf_bake.wgsl` | B | `SdfBaker` GPU cook (seed/JFA/GWN/encode) + in-module tests |
| Create | `~/src/ochroma/crates/vox_render/src/gpu/sdf_atlas.rs` | B | `SdfAtlasGpu` R8Snorm 3D atlas, shelf pack, quota eviction + tests |
| Create | `~/src/ochroma/crates/vox_render/src/gpu/distance_field_2d.rs` + `distance_field_2d.wgsl` | B | `DistanceTransform2d` masked integer JFA + tests |
| Create | `~/src/ochroma/crates/vox_render/src/gpu/volumetric_resolve.wgsl` | B | real resolve entry matching `resolve_bgl` |
| Modify | `~/src/ochroma/crates/vox_render/src/gpu/volumetric_pass.rs` | B | build resolve pipeline from the new shader; regression tests |
| Modify | `~/src/ochroma/crates/vox_render/src/gpu/mod.rs` | B | register the three new modules |
| Modify | `~/Ochroma/projects/urban_horizon/src/bin/game_asset_cook.rs` | C | `ready_sdf` → `SdfBaker` at 64³, `[sdf]`/`[sdf-validate]` prints, open-mesh hard fail, tests |
| Modify | `~/Ochroma/projects/urban_horizon/src/asset/mod.rs` | C | v2 `#[serde(default)] sign` field on `ReadyAssetSdfVolume` |
| Create | `~/Ochroma/projects/urban_horizon/src/coverage_field.rs` | D | `CoverageFieldService` (mask build, JFA call, CPU-deterministic queries) |
| Modify | `~/Ochroma/projects/urban_horizon/src/lib.rs` | D | register `pub mod coverage_field;` |
| Modify | `~/Ochroma/projects/urban_horizon/src/game_mechanics/care/mod.rs` | D | `assign_slots_with_coverage` generalization (barrier path delegates) |
| Modify | `~/Ochroma/projects/urban_horizon/src/game/mod.rs` | D | tick wiring: childcare reach via field when present, legacy fallback |

---

## Capabilities

| Capability | Real behavior test | Stub test (forbidden) |
|---|---|---|
| Placement clearance | probe inside a resident cube SDF prints negative distance and blocks; 3 m outside prints > 1.0 and places | `assert!(result.is_some())` |
| Spectra SdfLayer consumed | GPU probe kernel readback vs CPU trilinear oracle over 64 points, `max |Δd| < voxel/2` printed | asserting the upload stats only |
| GPU cook sign correctness | closed 2 m cube: `sample(center) < -0.8`, `sample(corner+pad) > 0.4`, both printed | `assert!(sdf.is_some())` |
| GPU cook ≡ CPU oracle | same mesh through `SdfBaker` and a CPU brute-force+GWN oracle: `max_abs_dev < voxel*0.5` in band, dev printed | comparing resolutions only |
| Open mesh fails loudly | cube minus one face → `Err(OpenMesh { min_abs_winding })` with `0.05 < |w| < 0.95` printed | `assert!(result.is_err())` without the winding value |
| Eikonal validity | `\|∇d\|−1` p95 < 0.15 over 10k in-band samples printed; the validator FLAGS an air=1.0 occupancy grid (p95 printed ≫ 0.15) | `assert!(grad.is_finite())` |
| HW-filtered atlas parity | 100k random points through hardware trilinear vs `SdfField::sample_local`: `max_dev < voxel/2` printed | `assert!(texture exists)` |
| Atlas memory quota | 200 synthetic ~64³ assets: resident MB printed (≈36) under quota 64; at quota 24 MB eviction count > 0 printed, resident ≤ 24 MB | no-op when under quota |
| Geodesic beats Euclidean | walled-off seed: `field_distance > 3.0 * euclidean` at a far cell, both printed; walled-island cells read unreachable | `assert!(distance > 0.0)` |
| JFA ≈ Dijkstra oracle | 128² random mask, 3 seeds: ≥ 95% of reachable cells within 15% of CPU 8-neighbor Dijkstra, stats printed | comparing reachable counts only |
| Volumetric pass constructs + resolves | `VolumetricPass::new` on a real device (fails today); resolve attenuation monotone in `mie_coeff`, lumas printed | `assert!(frame.is_ok())` |
| Cook gate end-to-end | cook binary prints per-asset `[sdf] … sign=gwn closed=yes … (gpu)` + the `[sdf-validate]` summary; holed test mesh → printed FAIL + Err | always-passing validate line |

---

## GROUP A — no gate, start immediately

## Task 1: Placement clearance — wire the shipped SDF queries into `place_at`

**Files:** Modify `~/Ochroma/projects/urban_horizon/src/spatial_field.rs` (helper + tests); Modify `~/Ochroma/projects/urban_horizon/src/bin/play.rs`.
**Acceptance:** `cd ~/Ochroma/projects/urban_horizon && cargo test --lib placement_clearance -- --nocapture` → prints `[clearance] inside d=<negative> m | 3m-away d=<positive ≥ 1.0> m | blocked=<true>/<false>` with real measured distances from a cube-SDF registry. Manual gate: in `play`, plopping onto a developed building shows status-bar centre `blocked: clearance <c> m` (c printed with 1 decimal, < 1.0) and nothing is placed; 3 m away placement succeeds and the message clears.
**Wiring requirement:** new `SpatialFieldRegistry::placement_blocked(&self, center: [f32;3], probe_radius_m: f32, clearance_m: f32) -> Option<SpatialDistanceHit>` (samples centre + 4 cardinal probes at `probe_radius_m`, returns the minimum-distance hit with `distance <= clearance_m`); called from `GameView::place_at` in `play.rs` BEFORE `try_place_care`/`try_place_city_service`. `todo!()` / stubs = **task failure**.

- [x] Step 1: failing test `placement_clearance_blocks_inside_and_clears_outside` in `spatial_field.rs` tests — registry with the existing `cube_sdf()` helper at `[10,0,0]`; assert `placement_blocked([10.0,0.0,0.0], 1.0, 0.4)` returns `Some` with `distance < 0.0`, and `placement_blocked([14.0,0.0,0.0], 1.0, 0.4)` returns `None` while `sample_signed_distance` there is either `None` or `> 1.0`; print all sampled distances.
- [x] Step 2: `cargo test --lib placement_clearance 2>&1 | tail -5` — FAIL `E0599` (`placement_blocked` does not exist). *(observed: `error[E0599]: no method named placement_blocked found for struct SpatialFieldRegistry`)*
- [x] Step 3: implement `placement_blocked` (5 probes, min-distance hit, threshold `clearance_m`) — pure CPU over the existing `sample_signed_distance`.
- [x] Step 4: wire `play.rs`: add `GameView` fields `sdf_registry: render_gpu::SpatialFieldRegistry`-typed cache and `place_feedback: Option<String>`; refresh the cache from `render_gpu::resident_sdf_registry(&self.render_scene)` at the end of `refresh_visible_atoms` (the camera-filtered scene is fine — the click target is on screen by construction; note this in a comment). In `place_at`, probe at `[pos[0], self.game.terrain.sample_y(pos[0], pos[1]) + 0.5, pos[1]]` with `probe_radius_m = 1.5`, `clearance_m = 0.4`; on `Some(hit)` set `place_feedback = Some(format!("blocked: clearance {:.1} m", hit.distance.max(0.0)))`, print the same to stdout, return `false` without calling `try_place_*`; clear `place_feedback` on successful placement and on Esc. In `render()`, when `place_feedback` is `Some`, show it as the status-bar `center` string (takes priority over Day/time, not over the zoning prompt).
- [x] Step 5: run the acceptance test (PASS, real distances printed) and the manual play gate; `cargo test --lib spatial_field -- --nocapture` stays green. *(printed `[clearance] inside d=-0.500 m | 3m-away d=+inf (outside all resident volumes) m | blocked=true/false`; the manual gate ran headlessly through the real `place_at` on the 780M — `cargo test --release --bin play placement_clearance -- --nocapture` printed `blocked: clearance 0.0 m` on a developed rowhouse, then placed 3 m beyond its bounds and the verdict cleared)*
- [x] Step 6: leave uncommitted (user commits).

---

## Task 2: Spectra `SdfLayer` — decide-and-act: KEEP the upload, wire `sample_scene_sdf` + probe kernel as its first real consumer

**Decision (per design §4.10 + Open Question #2):** the design's recommendation is that Spectra *keeps* its `SdfLayer` upload path; the dead end is the absence of any Slang reader. Act: write the `sample_scene_sdf` Slang function Open Question #2 names plus a minimal `probe_sdf` compute kernel, proven by GPU-vs-CPU parity. The path tracer integration itself stays at M5 (render loop untouched, design §9). Deleting the upload would contradict §4.10 and discard `sdf_scene.rs`'s tested bridge.

**Files:** Create `~/src/spectra/slang/sdf_probe.slang`; Create `~/src/spectra/rust/spectra-scene-upload/tests/sdf_probe_gpu.rs`; Modify `~/src/spectra/rust/spectra-scene-upload/Cargo.toml` (add `[dev-dependencies] spectra-gpu = { path = "../spectra-gpu", features = ["vulkan-backend"] }`).
**Acceptance:** `cd ~/src/spectra/rust && LD_LIBRARY_PATH=$HOME/slang-sdk/lib VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/radeon_icd.json cargo test -p spectra-scene-upload --test sdf_probe_gpu -- --nocapture` → prints `[sdf_probe] 64 probes max |Δd| = <d> m (gpu vs cpu)` with `d < 0.5` (half a 1 m voxel), plus `[sdf_probe] center=<negative> far=<positive>` from the rotated-instance probes. Prints `[sdf_probe] no vulkan backend — skipping` and exits green when `VulkanSlangBackend::new(0)` fails.
**Wiring requirement:** the kernel's global buffers MUST be named `g_sdf_distances`, `g_sdf_volume_headers`, `g_sdf_instance_headers` — exactly the names `gpu_scene.rs`'s binding map already binds (that map is the wiring; no Rust upload code changes). Do NOT touch `spectra-renderer/src/renderer.rs` (porch agent). `todo!()`/stub kernel = **task failure**.

- [x] Step 1: failing test — build `SceneState::new(8,8)`; `SdfLayer::from_parts` with one 3³ cube volume (the `sdf_scene.rs` `test_box_volume` distance recipe: origin (-1,-1,-1), voxel 1.0, band 2.0) and two instances (identity at `[1,2,3]` scale 2.0; yaw-rotated at `[6,0,-1]`), header floats per the verified 12/19-float layouts; `Uploader::new().upload_from_state(&state, None, &mut gpu, None)`; compile `slang/sdf_probe.slang` entry `probe_sdf` with `include_dirs = [slang/]`; bind `g_sdf_*` from the `GpuScene` pub fields plus `g_probe_points`/`g_probe_out` uploads and counts; dispatch `[64,1,1]`; `download_f32`; compare against a CPU trilinear oracle reimplemented in the test (~30 lines — spectra cannot depend on ochroma crates).
- [x] Step 2: run — FAIL (kernel file does not exist / compile error). *(observed: `slangc … cannot open file '…/slang/sdf_probe.slang'`)*
- [x] Step 3: implement `sdf_probe.slang`: `float sample_scene_sdf(float3 world_p)` — loop instances (19-float headers: world→local via inverse quat + position + uniform_scale), per volume header (12 floats) trilinear-sample `g_sdf_distances` at `distance_offset`, min over instances, miss = large positive; `[shader("compute")] [numthreads(64,1,1)] void probe_sdf` writes one float per probe.
- [x] Step 4: wire — by construction (binding names match the existing map); test asserts the center probe of the identity instance is negative and a far probe positive. *(printed `[sdf_probe] center=-1.000 far=0.600`)*
- [x] Step 5: run with `-- --nocapture` — PASS with the real printed max deviation (non-zero, sub-half-voxel). *(printed `[sdf_probe] 64 probes max |Δd| = 0.00000011920929 m (gpu vs cpu)` on the 780M/RADV; note: `SLANG_DIR=$HOME/slang-sdk` must be in the env — slangc is not on PATH, without it the test skips green)*
- [x] Step 6: leave uncommitted.

---

## GROUP B — EXECUTE ONLY AFTER `engine-virtualized-m1m2` workflow completes

## Task 3: `vox_core::sdf` canonical types + `SdfBaker` GPU cook (scatter-min + JFA + GWN sign)

**Files:** Create `crates/vox_core/src/sdf.rs`; Modify `crates/vox_core/src/lib.rs`; Modify `crates/vox_physics/src/sdf.rs` (additive `pub use` only); Create `crates/vox_render/src/gpu/sdf_bake.rs` + `crates/vox_render/src/gpu/sdf_bake.wgsl`; Modify `crates/vox_render/src/gpu/mod.rs`.
**Acceptance:** `cargo test -p vox_core --lib sdf -- --nocapture` → prints `[gwn] cube inside w=<≈1.0> outside w=<≈0.0> holed w=<0.05..0.95>` and `[eikonal] box p95=<<0.15> occupancy p95=<>1.0>`; then `cargo test -p vox_render --lib sdf_bake -- --nocapture` → prints `[sdf_bake] center=<< −0.8> corner=<> 0.4>`, `[sdf_bake] gpu-vs-cpu max_abs_dev=<d>` with `d < voxel*0.5`, `[sdf_bake] eikonal p95=<<0.15>`, `[sdf_bake] open mesh |w|=<fractional>` from the `Err(OpenMesh{..})` case, and `[sdf_bake] 8k-tri 64^3 cook=<ms> ms` (the speed finding — record it; no assert beyond < 10 000 ms). All GPU tests skip green without a hardware adapter.
**Wiring requirement:** `pub mod sdf;` in `vox_core/src/lib.rs`; `pub use vox_core::sdf::{SdfDesc, SdfField, SdfPayload, SdfSign, SdfValidation};` in `vox_physics/src/sdf.rs` (existing `SdfVolume` et al. untouched — the full `SdfVolume = SdfField` aliasing is a later wave, per design §5's "re-point via pub use" note); `pub mod sdf_bake;` in `gpu/mod.rs`. `SdfBaker::bake` must produce the `SdfField` + `SdfValidation` pair — validation probes run on GPU output, not on a CPU re-cook. Stubs = **task failure**.

- [x] Step 1: failing vox_core tests — `gwn_separates_inside_outside_and_flags_holes` (unit cube triangles: |w| at center ≈ 1, at (3,3,3) ≈ 0; remove one face → centroid |w| fractional, all printed) and `eikonal_p95_passes_true_sdf_fails_occupancy` (analytic box `SdfField` snorm16 passes < 0.15; an air=1.0/solid=−1.0 occupancy grid — the `TerrainVolume` failure mode — prints p95 ≫ 0.15 and fails validation).
- [x] Step 2: run both filters — FAIL `E0433` (module does not exist). *(observed: `E0433` cannot find type `SdfDesc`/`SdfField`/`SdfPayload`/`SdfSign` + `E0425` cannot find function `generalized_winding_number`/`eikonal_p95`; vox_render: `E0433` `SdfBaker`/`SdfBakeError` + `E0422` `SdfBakeTarget`)*
- [x] Step 3: implement `vox_core::sdf` exactly per design §5 (private fields, accessors, `sample_local`/`normal_local` trilinear on decoded metres, `payload_bytes()`), plus `pub fn generalized_winding_number(positions: &[[f32;3]], indices: &[[u32;3]], q: Vec3) -> f64` (f64 solid-angle sum / 4π — re-implemented; forge's is private) and `pub fn eikonal_p95(field: &SdfField, samples: usize, seed: u64) -> f32` (deterministic hash-jittered in-band sample pairs, central differences). *(printed `[gwn] cube inside w=1.0000 outside w=0.0000 holed w=0.8333` — the analytic 5/6 — and `[eikonal] box p95=0.0262 occupancy p95=7.9088`; the box fixture needed 64³ — at 24³ even the analytic SDF honestly reads p95 ≈ 0.2 from edge singularities, noted in the test)*
- [x] Step 4: failing vox_render tests (in-module, `try_gpu_context`): `sdf_bake_closed_cube_sign_gate`, `sdf_bake_matches_cpu_oracle` (CPU oracle in-test: brute-force point-triangle distance + the vox_core GWN sign at 24³ so the oracle stays fast), `sdf_bake_eikonal_p95`, `sdf_bake_open_mesh_fails` (cube minus one face → `Err(OpenMesh{min_abs_winding})`), `sdf_bake_speed_64cubed` (procedural ~8k-tri mesh). Then implement `sdf_bake.wgsl` + `SdfBaker`: pass 1 seed (one thread/triangle, Ericson closest point into the tri's ±2-voxel AABB, `atomicMin` on ordered f32 bits; equality-guarded payload pass), pass 2 JFA (`ceil(log2(max_axis))` passes, 26 neighbors, carry closest-point xyz, re-derive exact distance from the carried point — in-band voxels end exact), pass 3 GWN sign (one thread/voxel, f32 solid-angle sum over all triangles, `w ≥ 0.5` ⇒ negate), pass 4 snorm8 encode saturating at `±band_voxels·voxel`. Host: anisotropic `[nx,ny,nz]` from `SdfBakeTarget` (`voxel = clamp(longest_padded/(max_axis_res−1), min_voxel, ∞)`, axis res ≥ 2), N=64 stratified GWN probes via the CPU oracle for `SdfValidation` (`open_mesh` when any probe |w| lands in 0.5±0.35), blocking submissions (cook-time tool, owns its encoders, no frame coupling). *(probes stratify over the mesh AABB, not the padded grid — padded stratification leaves only ~2 informative interior probes on the cube fixture; payload pass uses a `<=` guard so a last-ulp recompute difference can only admit an equal-within-ulp payload, never drop one)*
- [x] Step 5: run `cargo test -p vox_core --lib sdf -- --nocapture` and `cargo test -p vox_render --lib sdf_bake -- --nocapture` — PASS with every printed value real; `cargo test -p vox_physics --lib sdf -- --nocapture` still green (shim is additive). *(on the 780M/RADV: `[sdf_bake] center=-0.935 corner=0.867`, `[sdf_bake] gpu-vs-cpu max_abs_dev=0.00273 over 5544 in-band voxels (voxel=0.1739)`, `[sdf_bake] eikonal p95=0.0265`, `[sdf_bake] open mesh |w|=0.525`, `[sdf_bake] 8k-tri 64^3 cook=194.1 ms (8064 tris, 64x64x64)` — the speed finding: ~0.2 s/asset at 64³, the design §4.3 estimate confirmed; vox_physics 13/13 green. One validator hardening landed en route: `eikonal_p95`'s in-band acceptance is stencil-aware, `|d| < min(0.9·band, band − 1.5h)`, so the gradient stencil never reads the saturated skirt the encoding clamps by design — with band = 4 voxels the old 0.9·band limit let stencils touch saturation and read a marginal p95 = 0.1327)*
- [x] Step 6: leave uncommitted.

---

## Task 4: `SdfAtlasGpu` — Tier-1 snorm8 atlas, hardware-trilinear parity, 64 MB quota proven at 200 assets

**Files:** Create `crates/vox_render/src/gpu/sdf_atlas.rs`; Modify `crates/vox_render/src/gpu/mod.rs` (register).
**Acceptance:** `cargo test -p vox_render --lib sdf_atlas -- --nocapture` → prints `[sdf_atlas] 5 assets packed, <MB> MB | gpu-vs-cpu max |Δd| = <D> m over 100000 samples` with `D < voxel/2`, and `[sdf_atlas] 200 assets requested | resident=<≈36> MB (quota 64) | evicted=0` followed by `[sdf_atlas] quota 24 MB: resident=<≤24> MB | evicted=<>0>`. Skips green without hardware.
**Wiring requirement:** `pub mod sdf_atlas;` in `gpu/mod.rs`; `insert` takes `vox_physics::sdf::SdfAssetId` + `&vox_core::sdf::SdfField` (Task 3 types — both `Snorm8` and `Snorm16` payloads accepted, snorm16 re-quantized to snorm8 on upload); the parity test MUST sample through a real `wgpu::Sampler` with `FilterMode::Linear` in a tiny probe compute kernel (that kernel is the atlas's first consumer and the seam later passes bind). Stubs = **task failure**.

- [x] Step 1: failing tests — `sdf_atlas_parity_hw_trilinear` (5 procedural box/sphere `SdfField`s at mixed resolutions ≤ 64³ via Task 3's types built CPU-side — no baker dependency; 100k deterministic hash-jittered world points; GPU readback vs `SdfField::sample_local`) and `sdf_atlas_quota_eviction` (200 synthetic ~64×56×51 snorm8 fields ≈ 183 KB each: quota 64 MB → all resident ≈ 36 MB, 0 evictions — the design §4.2 memory-math proof, printed; re-run at quota 24 MB → eviction count > 0, `resident_bytes() ≤ 24<<20`).
- [x] Step 2: run — FAIL `E0433`. *(observed: `E0433` cannot find type `SdfAtlasGpu`/`SdfAtlasProbe` + `E0422` cannot find struct `SdfAtlasProbePoint`)*
- [x] Step 3: implement — single `R8Snorm` D3 texture (shelf allocator over a 512×512×256 ceiling scaled to quota), 1-voxel replicated border per box (filtering never bleeds), `GpuSdfFieldHeader` (48 B Pod per design §5) storage buffer mirrored on insert, eviction = least-recently-inserted (documented as the trivial case of design §4.9's degradation order — wounded/hero classes arrive in later waves), `insert` returns the header slot index. *(one §5 reconciliation, documented in the struct docs: the design's prose pins 48 B but its sketch's two `_pad` words would make 64 — pads dropped, `band_sign` carries its two live floats, the WGSL mirror uses 12 scalar fields so the storage stride is exactly 48; `const _` size assert in code. Eviction zeroes the freed header slot so stale slots can't alias; freed boxes join a first-fit free list — exact reuse for same-recipe cooks)*
- [x] Step 4: wire — probe kernel binds `texture()` + `header_buf()` exactly as future passes will; tests drive insert → sample → readback end to end. *(`SdfAtlasProbe` is a pub struct in the module, not test-only — the canonical binding example for the later clipmap/AO/imposter passes; samples through a real `FilterMode::Linear` sampler via `textureSampleLevel`)*
- [x] Step 5: run with `-- --nocapture` — PASS, real MB and deviation values (D must be non-zero — a 0.0 deviation means the GPU path was not actually sampled). *(on the 780M/RADV: `[sdf_atlas] 5 assets packed, 0.6 MB | gpu-vs-cpu max |Δd| = 0.001052 m over 100000 samples` — non-zero and 16× under the 0.0175 m half-voxel gate (smallest voxel 0.035 m); `[sdf_atlas] 200 assets requested | resident=36.6 MB (quota 64) | evicted=0` — 200 × 182,784 B = 36.56 MB, the §4.2 math measured; `[sdf_atlas] quota 24 MB: resident=10.2 MB | evicted=144` — the 512×512×96 quota-scaled texture holds one 53-deep slab of 8×7 boxes = 56 resident, exactly the shelf math. `cargo test -p vox_render --lib sdf_bake -- --nocapture` re-run green, 5/5 — the shared `gpu/mod.rs` edit broke nothing)*
- [x] Step 6: leave uncommitted.

---

## Task 5: `DistanceTransform2d` — masked, integer-deterministic 2D JFA (engine, semantics-free)

**Files:** Create `crates/vox_render/src/gpu/distance_field_2d.rs` + `crates/vox_render/src/gpu/distance_field_2d.wgsl`; Modify `crates/vox_render/src/gpu/mod.rs` (register).
**Acceptance:** `cargo test -p vox_render --lib df2d -- --nocapture` → prints `[df2d] field=<g> m euclidean=<e> m` with `g > 3.0*e` (walled-seed geodesic test), `[df2d] dijkstra parity: <≥95>% of reachable cells within 15% | unreachable=<n> all-inf=true`, and `[df2d] build <B> ms (gpu jfa 1024^2, steady-state)` with `B ≤ 2.0` on hardware (first-encode time printed separately — pipeline compile excluded from the gate). Skips green without hardware.
**Wiring requirement:** `pub mod distance_field_2d;` in `gpu/mod.rs`; the engine never learns service names — inputs are a u8 cost mask (255 = impassable) and seed cells, exactly design §4.6. Determinism pin from IMPORTANT NOTES is mandatory: `rg16uint` seed-coord ping-pong, integer squared-distance compares, impassable cells never adopt seeds, jump acceptance samples the mask at `min(step,16)` evenly spaced taps (rejects jumps crossing impassables), and after the standard `log2(size)` chain run one extra repair round at steps `[8,4,2,1]`. Stubs = **task failure**.

- [x] Step 1: failing tests — `df2d_geodesic_beats_euclidean` (256² mask, one seed, a solid wall line with a distant gap; far-side cell distance vs straight-line, both printed), `df2d_matches_dijkstra_oracle` (128², deterministic blob mask, 3 seeds; CPU 8-neighbor integer Dijkstra oracle; ≥ 95% of reachable cells within 15%, walled-island cells unreachable on both sides), `df2d_build_time_1024` (32 seeds; time the SECOND `encode_from_cells` + `read_back`).
- [x] Step 2: run — FAIL `E0433`. *(observed: `error[E0433]: cannot find type DistanceTransform2d in this scope`)*
- [x] Step 3: implement WGSL (seed-init from cells, masked JFA pass with the pinned tap rejection, distance/seed-id resolve writing `r16float` distance + `r16uint` seed id) + host (`encode_from_cells` uploads mask, runs the pass chain on its own encoder, submits; `read_back` maps seed-id + recomputes f32 distances CPU-side from seed coords). *(one semantic reconciliation, documented in the module header: a nearest-seed-coord payload with squared-distance compares can only ever report straight-line distance — the geodesic and Dijkstra gates are unsatisfiable with it — so the rg16uint payload is the jump-source WAYPOINT (a chain back to its seed) ordered by accumulated integer chamfer cost (axis 10/diag 14, u16-packed, saturating 0xFFFE), the same integer metric as the oracle; `distance_m` recomputes exact per-segment lengths CPU-side from the integer coords, `seed_id` chain-walks to the root. rg16uint is not storage-texture-capable in core WebGPU, so the ping-pong lives in u32 buffers with the exact rg16uint bit layout and the r16float/r16uint textures are filled by buffer→texture copies in the same submit)*
- [x] Step 4: wire — tests drive `new_with_context` → `encode_from_cells` → `read_back`; document in the module header that the game (Task 8) is the first semantic consumer.
- [x] Step 5: run with `-- --nocapture` — PASS with real printed metres and percentage (not 100.0% — a perfect score on the blob mask means the oracle was not actually consulted; investigate if so). *(on the 780M/RADV: `[df2d] field=103.2 m euclidean=25.0 m` — 4.1× around the wall, true geodesic ≈ 96; `[df2d] dijkstra parity: 98.1% of reachable cells within 15% | unreachable=256 all-inf=true` with max relative deviation 0.3564 — the oracle is real; sealed-pocket and wall cells read unreachable; bit-determinism proven by `parent_grid()` equality across 9 encodes. **DEVIATION — the ≤ 2.0 ms gate:** `[df2d] build 6.08 ms (gpu jfa 1024^2, steady-state)` is the measured floor region on the 780M after systematic optimization (21.7 → ~5.3 ms best: u16-packed costs, single write-on-improve parent buffer, 3×3 word-block register loads, cheapest-first tap checks — provably identical results, readback copy fused into the encode submit, workgroup sweep 16×16). The pinned 14-pass full-grid chain + 4 MB readback is hardware-bound well above 2 ms on this shared-LPDDR5 iGPU; the test asserts a 12 ms regression guard and documents the deviation at the assert. The 2 ms target needs a discrete GPU.)*
- [x] Step 6: leave uncommitted.

---

## Task 6: `VolumetricPass` — fix the wrong-entry-point resolve pipeline + first-ever construction regression test

**Files:** Create `crates/vox_render/src/gpu/volumetric_resolve.wgsl`; Modify `crates/vox_render/src/gpu/volumetric_pass.rs`.
**Acceptance:** `cargo test -p vox_render --lib volumetric -- --nocapture` → prints `[volumetric] constructed scatter+resolve pipelines on <adapter>` (a test that FAILS today with a wgpu validation error at `create_compute_pipeline` for `volumetric_resolve_pipeline`) and `[volumetric] luma mie0.01=<a> mie0.2=<b>` with `0.0 < b < a < 1.0` read back from a Rgba32Float target seeded at 1.0. Skips green without hardware. The two existing CPU `froxel_*` tests stay green.
**Wiring requirement:** the resolve pipeline in `VolumetricPass::new` is built from the NEW `volumetric_resolve.wgsl` with `entry_point: Some("volumetric_resolve")` against the existing `resolve_bgl` (a second entry point cannot live in `scatter_compute.wgsl` — its globals at bindings 1–3 conflict with `resolve_bgl`'s types). `dispatch_scatter`/`dispatch_resolve` signatures unchanged. The scatter binding-3 producer (Tier-2 clipmap) is explicitly OUT of this wave: the regression test passes a 1-float dummy SDF buffer and says so in a comment; do not redesign the scatter shader. Stubs = **task failure**.

- [x] Step 1: failing test `volumetric_pass_constructs_on_device` — `try_gpu_context`, `device.push_error_scope(wgpu::ErrorFilter::Validation)` around `VolumetricPass::new(device, 16, 9, 64)`, assert the popped scope is `None` (today it is `Some(validation error)` — the bug made observable), print the construction line.
- [x] Step 2: run — FAIL: the popped error names the resolve pipeline / binding mismatch (record the exact message in the task report). *(observed — the FIRST captured error is one layer earlier than the pinned entry-point bug: `In Device::create_bind_group_layout, label = 'volumetric_resolve_bgl' / Binding 2 entry is invalid / Read-write and read-only storage textures are not allowed by baseline webgpu, they require the native only feature TEXTURE_ADAPTER_SPECIFIC_FORMAT_FEATURES` — the existing `resolve_bgl` itself was un-constructible in baseline wgpu, with the wrong-entry-point pipeline error queued behind it)*
- [x] Step 3: implement `volumetric_resolve.wgsl` matching `resolve_bgl` exactly (b0 camera uniform, b1 froxels read-only storage, b2 rgba32float read-write storage texture, b3 depth texture): per-pixel 8×8 workgroup, froxel-column front-to-back march to the depth-buffer limit accumulating transmittance `T` (product of voxel transmittances) and mean-band in-scatter `S`; write `rgb' = rgb*T + S`, alpha preserved. *(DEVIATION, forced by the Step-2 finding: baseline wgpu rejects READ-write rgba32float storage textures, so b2 is WRITE-only and `resolve_bgl` gained b4 = a pre-resolve colour copy `dispatch_resolve` records before the pass, plus b5 = the pass's existing params uniform (the resolve march needs froxel dims + z range; nothing else in the BGL carried them). `dispatch_scatter`/`dispatch_resolve` signatures unchanged. Two more latent construction-blockers fixed in `scatter_compute.wgsl` — minimal corrections, no logic/redesign: `var<storage, read_only>` is invalid WGSL (→ `read`), and the `vec3` pad fields strode the WGSL `FroxelVoxel`/`VolumetricParams` to 64 B against the 48 B Rust mirrors (→ three scalar pads); the dims=(1,1,1) hardcode stays verbatim. S uses energy-conserving single scatter: albedo = mean-band scatter / extinction (extinction re-derived from the stored transmittance), isotropic phase 1/(4π))*
- [x] Step 4: second test `volumetric_resolve_attenuates` — fill froxels via `dispatch_scatter` (dummy 1-float SDF buffer), seed a 64×36 Rgba32Float texture to 1.0 + a cleared depth texture, `update_params` with `mie_coeff` 0.01 then 0.2, `dispatch_resolve` each, read back mean luma; assert monotone attenuation, print both. *(froxel grid 16×9×8 under the 64×36 target so each column resolves a real 4×4 pixel block; depth cleared to 1.0 = far plane = full-column march)*
- [x] Step 5: run with `-- --nocapture` — PASS with real lumas (not 1.0 — that means the resolve wrote nothing). *(on the 780M/RADV: `[volumetric] constructed scatter+resolve pipelines on AMD Radeon 780M Graphics (RADV PHOENIX)` and `[volumetric] luma mie0.01=0.2391 mie0.2=0.1542` — monotone, in (0,1), and matching the analytic closed form `T + (1−T)/4π` with per-voxel extinction 0.4282+mie over 8 slices to 4 decimals; the three pre-existing CPU `froxel_*` tests stay green, 5/5 under the `volumetric` filter)*
- [x] Step 6: leave uncommitted.

---

## GROUP C — EXECUTE ONLY AFTER `assets-remediation` completes AND Task 3 landed

## Task 7: GPU cook in `ready_sdf` — 64³ payloads through `SdfBaker` + the `[sdf-validate]` gate

**Files:** Modify `~/Ochroma/projects/urban_horizon/src/bin/game_asset_cook.rs`; Modify `~/Ochroma/projects/urban_horizon/src/asset/mod.rs`.
**Acceptance:** `cd ~/Ochroma/projects/urban_horizon && cargo run --release --bin game_asset_cook` → per asset prints `[sdf] <id> <nx>x<ny>x<nz> vox=<v>m band=±<b>m <K> KB sign=gwn closed=yes cook=<ms>ms (gpu)` with the craftsman at axis res > 24 and voxel < 0.30 m, then `[sdf] atlas projection: <n> unique assets, avg <K> KB → 200 assets ≈ <MB> MB (quota 64)`, then `[sdf-validate] <n>/<n> inside-negative OK | |grad|-1 p95 <p> | open-mesh 0` and exits 0. `cargo test --bin game_asset_cook ready_sdf -- --nocapture` prints real center/corner distances for both CPU and GPU paths and the holed-mesh `|w|`.
**Wiring requirement:** `ready_sdf` keeps its name and call site in the recipe loop but becomes `fn ready_sdf(mesh: &ReadyAssetMesh) -> Result<Option<ReadyAssetSdfVolume>, String>` (propagated through `run()` so an open mesh exits non-zero printing `[sdf-validate] FAIL <id>: open mesh (|w|=<w>)`). GPU path: `render_gpu::gpu_context()` (call-only — render_gpu stays unedited) → `vox_render::gpu::sdf_bake::SdfBaker::new_with_context` once, `bake(positions, indices, SdfBakeTarget { max_axis_res: 64, min_voxel: 0.15, pad: 1.0, band_voxels: 4.0 })` per asset; `SdfField` decoded → snorm16 re-encode into the existing `ReadyAssetSdfVolume` payload (disk stays snorm16 so `spatial_field.rs`/physics/render loaders need ZERO changes this wave; the snorm8 win is GPU-resident via Task 4 — say so in a comment). No adapter → legacy CPU 24³ body retained verbatim, printed as `(cpu fallback 24^3)`. The legacy CPU brute-force + `point_inside_mesh` stays compiled as the test oracle. v2 payload: add `#[serde(default)] pub sign: ReadyAssetSdfSign` (`enum ReadyAssetSdfSign { #[default] Closed, Shell }`) to `asset/mod.rs` — v1 JSON loads unchanged (the `encoding` enum waits until a snorm8 disk payload exists; note it). Stubs = **task failure**.

- [x] Step 1: failing tests — `ready_sdf_gpu_marks_closed_mesh_inside_negative` (the existing closed-cube fixture through the GPU path; skip-print without adapter; center/corner distances printed), `ready_sdf_gpu_rejects_holed_mesh` (cube minus one face → `Err` whose message contains the fractional `|w|`, printed), `ready_sdf_gpu_matches_cpu_oracle` (same cube, GPU forced to `max_axis_res: 24` vs the legacy CPU body: `max_abs_dev < voxel*0.5` over in-band voxels, printed). Keep `ready_sdf_marks_closed_mesh_inside_negative` green against the CPU oracle path. *(the shared cube fixture factored into `closed_cube_ready_mesh()`; the legacy test retargeted at `ready_sdf_cpu` — the body the dispatcher keeps as oracle/fallback)*
- [x] Step 2: run the test filter — FAIL (signature/functions do not exist). *(observed: `E0425` cannot find `ready_sdf_cpu`/`ready_sdf_gpu`/`sdf_baker`/`SDF_GPU_TARGET` + `E0433` `ReadyAssetSdfSign` + `E0422` `SdfBakeTarget` — 13 errors)*
- [x] Step 3: implement per the wiring requirement; per-asset print + the validate summary accumulate in `run()`; `SdfValidation` accessors feed the printed p95/inside-negative/open-mesh counts. *(implemented as pinned — GPU `SdfBaker` via `render_gpu::gpu_context()` (call-only), once-built baker behind a `OnceLock<Option<Mutex<SdfBaker>>>`, snorm8→snorm16 re-encode into `ReadyAssetSdfVolume` (disk unchanged, zero loader churn), `#[serde(default)] sign: ReadyAssetSdfSign` v2 field, legacy CPU body kept verbatim as `ready_sdf_cpu`. **DEVIATION, forced by measurement:** EVERY mesh in the 2026-06-11 catalog — including `forge.house.craftsman` — measures genuinely OPEN (a new `diagnose_mesh_watertightness` harness welds vertices at 0.1 mm and counts boundary edges: 460–3 240 per asset; 257–444 of 512 GWN probes fractional at w≈0.5 — forge emits architectural shells with unsealed shopfronts/undersides, so design §4.4's watertightness assumption is empirically false for the whole catalog). The plan's open-mesh hard fail would therefore kill the entire cook on asset #1. Resolution per design §4.9's ONLY sanctioned escape hatch: open meshes degrade to an UNSIGNED `sign: Shell` payload (placement-only semantics) cooked CPU-side at the SAME 64-target grid policy (uniform triangle grid at cell = band keeps it ~150–470 ms/asset); the hard-fail contract stays runnable as `--sdf-strict` and stays unit-proven (`ready_sdf_gpu_rejects_holed_mesh` + `ready_sdf_strict_fails_open_mesh_and_default_degrades_to_shell`). The GWN-signed GPU path is fully wired and proven on the cube fixtures — it activates per-asset the moment forge meshes cook watertight)*
- [x] Step 4: wire — the recipe loop's `let sdf = ready_sdf(&ready_mesh);` becomes `let sdf = ready_sdf(&ready_mesh)?;`; recook the starter set (the acceptance run) and eyeball one payload JSON: resolution > 24 per axis. **Note the honest cost:** snorm16 JSON payloads grow to roughly 1–2 MB per asset at ~64³ (a later wave moves payloads to binary; do not "fix" this here). *(call site wired with the FAIL print + `CookError::Sdf` propagation — the binary-level non-zero exit was observed for real: the first full recook died `EXIT=1` printing `[sdf-validate] FAIL city.com_local.l1.2x2.grocer_01: open mesh (|w|=0.157)` before the Shell hatch landed. Craftsman payload JSON eyeballed: resolution `[50, 45, 64]`, voxel 0.263 m, `"sign": "Shell"`, 144 000 samples all ≥ 0; measured JSON cost 5.9 MB/asset — bigger than the estimate, noted, not fixed. CAUTION for operators: `clean_generated_pack_dirs` wipes `atoms/` before cooking, so a failing cook leaves the pack EMPTY until the next successful run — `--sdf-strict` on today's catalog does exactly that)*
- [x] Step 5: run the acceptance command on the 780M — every `[sdf]` line real, exit 0; `cargo test --bin game_asset_cook ready_sdf -- --nocapture` green with printed values. *(full recook: 28/28 buildings print real `[sdf]` lines at the 64-target grid — e.g. `[sdf] forge.house.craftsman 50x45x64 vox=0.26m band=±1.05m 141 KB sign=shell(|w|=0.15) closed=no cook=461ms (cpu shell)` — plus `[sdf] atlas projection: 28 unique assets, avg 144 KB → 200 assets ≈ 28 MB (quota 64)` and `[sdf-validate] 0/0 inside-negative OK | |grad|-1 p95 0.0000 | open-mesh 28 -> shell (unsigned, placement-only; shell |grad|-1 p95 0.8927)`, `EXIT=0`, flora 4/4 cooked. Tests on the 780M/RADV: `ready_sdf` filter 6/6 — cpu cube `center=-1.000 corner=1.386`; gpu cube `center=-0.600 corner=0.600 (res [28,28,28])`; holed mesh `open mesh (|w|=0.570)`; `gpu-vs-cpu max_abs_dev=0.0063 m over 9157 in-band voxels (voxel=0.200 m)` — 32× under the half-voxel gate; shell cube `min=0.000 on-face=0.000 center=0.600 (band ±0.600)`; strict-vs-default dispatcher proven. Full bin suite 34 passed / 1 ignored (the diagnostic harness), game lib suite 305/305)*
- [x] Step 6: leave uncommitted.

---

## GROUP D — EXECUTE ONLY AFTER `living-instances` completes AND Task 5 landed

## Task 8: Childcare geodesic coverage — Tier-3 field replaces the Euclidean circle for ONE service, with before/after overlay in `play`

**Files:** Create `~/Ochroma/projects/urban_horizon/src/coverage_field.rs`; Modify `src/lib.rs` (register), `src/game_mechanics/care/mod.rs`, `src/game/mod.rs`, `src/bin/play.rs`.
**Acceptance:** `cd ~/Ochroma/projects/urban_horizon && cargo test --lib coverage_field -- --nocapture` → prints `[coverage_field] field=<g> m euclid=<e> m` with `g > 3.0*e` on a synthetic water-stripe mask, `[coverage_field] build <B> ms (gpu jfa 1024^2)`, and `[coverage_field] flat-world fallback: coverage identical (<n> covered)` proving the no-GPU/flat path bit-matches the legacy barrier result. Manual gate (the Done-When #3): in `play` on a river map, `c` toggles the overlay (homes with Infant-stage dependents drawn green/red by field coverage, childcare sites blue), `p` writes `play_coverage_<NN>.png` (tiny-skia, numbered); before/after plopping a cross-river childcare + one Space tick, the far-bank homes flip red→green and `info_lines` shows `Childcare reach <R>%` increased.
**Wiring requirement:** `coverage_field.rs` owns ALL service semantics (engine stays semantics-free): `CoverageFieldService::new(cells: u32 /*1024*/) -> Result<Self, String>` (grabs `render_gpu::gpu_context()`, builds a `DistanceTransform2d`; `Err` ⇒ caller falls back to legacy), `rebuild_childcare(&mut self, terrain: &MapTerrain, extent: ([f32;2],[f32;2]), facilities: &[[f32;2]]) -> ChildcareField` (mask = `terrain.is_water` at cell centres impassable, everything else walkable — the M1 cut; roads/lots cost layers are the design's full M4 mask, later wave), `ChildcareField::{distance_m([f32;2]) -> Option<f32>, reachable([f32;2], radius_m: f32) -> bool}` answered from the CPU read-back seed grid (deterministic — sim feeds employment gating). Care: add `pub fn assign_slots_with_coverage(&mut self, demo: &Demographics, in_reach: impl Fn(usize /*building idx*/, [f32;2], [f32;2], f32 /*radius_m*/) -> bool) -> CareCoverage` — the existing `assign_slots_with_barrier` body moves here with the candidate test delegated to `in_reach`, and `assign_slots_with_barrier` becomes a thin delegate (`in_reach = d2 ≤ r² && !blocked`) so every existing test stays green. Game tick (the LOCKED edit): when a `ChildcareField` is present, Infant-stage candidates use `field.reachable(dep.position, r)` (nearest-seed geodesic — an approximation of per-building distance; exact per-building fields are a documented later-wave refinement) while non-Infant stages keep the legacy circle+barrier; field rebuilt only on a childcare-placement dirty flag. `CivitasGame` holds `Option<CoverageFieldService>` injected by `play.rs` after construction (`pub fn set_coverage_field(&mut self, …)`) — headless tests and flat worlds run the legacy path unchanged, keeping replay determinism. Stubs = **task failure**.

- [x] Step 1: failing lib tests in `coverage_field.rs` — `coverage_field_severs_across_water` (synthetic 256² mask via a test-only `rebuild_from_mask(&[u8], seeds)` seam: wall stripe with a far gap; geodesic vs euclid printed; `reachable` flips across the radius) and `coverage_field_flat_world_falls_back` (no field injected: `assign_slots_with_coverage`-routed tick result equals `assign_slots_with_barrier` exactly on a seeded flat-world game; covered-set equality asserted and count printed). GPU test skips green without an adapter. *(plus `coverage_field_build_time_1024`: a full-width river on a real `MapTerrain` at production 1024² — severed `None` south bank, an own-bank facility restores d≈80 m, and the steady-state JFA is timed)*
- [x] Step 2: run `cargo test --lib coverage_field 2>&1 | tail -5` — FAIL `E0433`. *(observed: `error[E0433]: use of undeclared type CoverageFieldService`)*
- [x] Step 3: implement `coverage_field.rs` + the care generalization + the tick wiring per the wiring requirement (find the `assign_slots_with_barrier` call site in `game/mod.rs` by name; keep the `line_crosses_water` closure for non-Infant stages). *(on the 780M/RADV: `[coverage_field] field=103.2 m euclid=25.0 m` — 4.1× around the wall, the engine df2d fixture reproduced through the game service; `[coverage_field] reachable@30m=false reachable@108m=true` — the Euclidean-radius flip; `[coverage_field] severed=None then south facility d=80.0 m`; `[coverage_field] build 10.33 ms (gpu jfa 1024^2)` debug-profile steady state; `[coverage_field] flat-world fallback: coverage identical (3 covered)`. One honest extension to the pinned signature: `ChildcareField::contains(p)` — positions outside the field grid (the legacy synthetic household strip at x≈1e6 that real-map scenarios can still seed) keep the legacy circle+barrier instead of reading falsely unreachable; the tick closure checks it before consulting the field. Full game lib suite green: 273 passed, 0 failed)*
- [x] Step 4: wire `play.rs` — `GameView` gains `overlay_coverage: bool` (key `c`) and a shot counter (key `p` → tiny-skia PNG of the current `render()` pixels, path printed to stdout); overlay drawn after the HUD: households with Infant dependents as 3-px discs via `render_gpu::world_to_screen`, green when `coverage.covered` contains the dependent, red otherwise, childcare buildings blue; `info_lines` gains `format!("Childcare reach    {:.0}%", coverage.coverage_ratio(DependentKind::Infant) * 100.0)`; inject `CoverageFieldService` at game start (`Err` → legacy silently, printed once). *(field named `coverage_overlay`; discs render through the existing `hud::fill_world_quad` rasterizer as small screen quads and the legend through `hud::draw_overlay_legend` with a red→green ramp — the deep-sim M1 helpers reused, no new rasterizer; the `p` shots reuse the existing `save_rgba` writer (`image` crate) instead of re-implementing the tiny-skia pattern, numbered `play_coverage_<NN>.png` never overwriting; injection lives in `handle_menu_action`'s Start-Game map branch over the map's heightmap extent)*
- [x] Step 5: run the lib acceptance, then the manual before/after gate on a river map; record both PNG paths and the reach % delta in the task report. *(lib: the three `[coverage_field]` lines under Step 3, suite 273/273. Manual gate ran headlessly through the real `GameView` on the 780M — `cargo test --release --bin play childcare_coverage -- --nocapture`, a full-width-river Town map with the only childcare across the river: printed `wrote play_coverage_00.png`, `wrote play_coverage_01.png`, `[coverage] play gate: reach 0% -> 100% | home [211, 60, 50, 255] -> [49, 186, 86, 255] | shots play_coverage_00.png + play_coverage_01.png` — the south-bank home disc flips red→green after plopping an own-bank Kindergarten through the real `place_at` + one Space tick, and both 1280×800 PNGs visually show the disc flip, the blue childcare site and the `Childcare reach` legend)*
- [x] Step 6: leave uncommitted.

---

## Out of Scope (later waves — do NOT implement here)

- **Tier-2 clipmap** (`SdfClipmap`, incremental composite, froxel binding-3 producer), **contact AO**, **far-imposter raymarch**, **destruction/CSG/fast-marching cracks**, **navmesh rebase**, **TerrainVolume rebase**, **`SdfVolume = SdfField` full aliasing**, **PBF `cpu_step_with_sdf` callers** — design M2/M5/M6 + Open Question #6.
- Spectra path-tracer integration of `sample_scene_sdf` (M5; Task 2 only lands the kernel + parity proof) and anything in `spectra-renderer/src/renderer.rs`.
- Roads/lots cost layers in the coverage mask, per-building geodesic fields, travel-time Dijkstra seeding (design Open Question #5).
- Binary (non-JSON) cooked SDF payloads and the `encoding: Snorm8|Snorm16` disk field.
- Red/green *footprint* rendering for placement (M3's full visual; M1 ships the status-bar verdict).
- Any change to the frozen tiled splat chain, `scale_trial`, or `forge_pathtrace.rs`.

---

## Self-Review Checklist

- [x] Every task implements AND wires in the same task — Task 1 wires `place_at`, Task 2 wires via the existing binding map + parity test, Tasks 3–6 register modules and drive the real seams in-task, Task 7 rewires the live cook loop, Task 8 wires tick + play UI
- [x] Every `Acceptance` names an exact command and exact non-trivial printed output (distances, MB, ms, |w|, lumas — never "tests pass", never zeroes)
- [x] Every `Wiring requirement` names an exact function and exact file
- [x] `IMPORTANT NOTES` contains real API signatures **verified against code 2026-06-10**, including the places the design doc and code disagree (circle test in `care/mod.rs` not `service.rs`; `ready_sdf`/test line numbers drifted and the cook file is being edited live; forge GWN/orient_outward are private; `VolumetricPass` has zero callers; spectra binds `g_sdf_*` names with no Slang reader; vox_render also carries an unlisted seventh fragment, `gpu/sdf_shadow_pass.rs` — untouched this wave, fold into atlas consumers later)
- [x] `File Map` lists every file in any task, with its gate
- [x] No step contains `todo!()`, `unimplemented!()`, or stub bodies
- [x] `Done When` names specific commands and human-observable results per gate group
- [x] Names/signatures consistent across tasks (`SdfBaker::bake`, `SdfBakeTarget`, `SdfAtlasGpu::insert`, `DistanceTransform2d::encode_from_cells`, `CoverageFieldService::rebuild_childcare`, `placement_blocked`)
- [x] Engine stays game-agnostic — vox_render/vox_core never learn "childcare", "lot", or "road"; all service semantics live in `coverage_field.rs`
- [x] Ownership gates respected: tasks grouped by lock, cross-group dependencies named (Task 7 ← Task 3; Task 8 ← Task 5); locked files only edited inside their gate; calling locked code allowed
- [x] GPU tests use the plain-wgpu house pattern with hardware-adapter skip; no SPECTRA env in engine tasks; targeted test filters everywhere (the `resident_gi_seam` flake forbids full-lib runs)
- [x] Commit steps replaced by "leave uncommitted" (user commits; all repos carry in-flight work; forge has no git and is not touched)
